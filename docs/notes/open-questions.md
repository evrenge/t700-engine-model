# Open questions

Anything the report does not answer goes here rather than into the code as a guess.
Each entry: what is missing, where it is needed, where it was looked for, and what
blocking it costs.

> **Numbering is not contiguous, and that is deliberate.** Four agents appended to this
> file concurrently and two collided on 14-17. Rows 18-21 are unused; the validation
> block was moved to 34-41 so that every cross-reference already written elsewhere still
> resolves. **Never renumber an existing row** — cite by number *and* by a few words of
> content, so a future collision is recoverable by reading rather than by guessing.

> **Tally as of 2026-09-12: 48 logged — 32 closed, 5 partly closed, 11 open.** The first
> word of the Status cell is the verdict and is kept truthful; six rows had theirs buried
> mid-cell until 2026-09-12, which made the ledger under-report its own progress.

| # | Question | Needed for | Searched | Status |
|---|---|---|---|---|
| 1 | Are the compressor / turbine maps printed as numeric tables in Appendix A, or only as plots that must be digitized? | Phase 1 data capture | pdf pp.55–66 read page by page from the images | **closed** — plots only. Appendix A holds exactly one numeric table (Table A.1, 33 scalar constants, pdf p.55) and eleven figures (A1–A11, pdf pp.56–66). All eleven functional relationships `f₁`–`f₁₀` and `f_hs` are printed **only** as x–y plots and must be digitized. See `inventory-appendix-a.md` |
| 2 | What exactly is the "opened" compressor mass-flow iteration, and how does it carry state across frames? | Phase 4 integration scheme | pdf pp.36-37 read from 600-dpi crops, equation by equation; pdf p.38 for the consequence; pdf p.47 for the rates actually used | **closed** — mechanism found on pdf p.36. Under the quasi-steady approximation P3 and P41 are solved algebraically each frame, and two nested loops appear: an **inner** loop coupling P3 and P41 across the combustor (Eq. 76 gives P3 from P41; Eq. 78 gives P41 from P3), and an **outer** loop through the compressor map, `P3 → Ps3 = K_Ps3·P3 → WA2_c = f₁(Ps3/P2, NG_c) → WA2 → {WA3, WA3_bl} → WA31 = WA3 − WA3_bl → P3`. Closing the outer loop would mean re-evaluating the compressor map several times per frame — the most expensive computation in the model. **"Opening" it means cutting that loop and running it exactly once per time step, with the combustor inlet flow built from a compressor mass flow carried over from the previous frame:** `WA31_(n) ≈ WA3_(n) − WA3_bl(n−1)` (Eq. 74, pdf p.36), the report's own words being "the outer loop was separated from the inner loop by assuming that the mass flow entering the combustor for a given interval is approximately equal to the mass flow leaving the compressor in the previous interval". **State carried across frames:** one mass flow, lb_m/sec — an extra saved scalar beyond the integrator states. **Initialization is not stated** (see the row on initializing the cross-frame mass flow, #23). **Why time-step dependent:** a converged closed loop would give the same answer for any Δt (algebraic loops have no memory); the opened loop replaces convergence with a one-frame delay, leaving a residual of order (dWA3/dt)·Δt — pdf p.37, "the approximation results in a value of iteration error which is a function of the time step". **The 0.1% criterion:** three separate statements, do not conflate — (a) pdf p.37, inner P3/P41 loop, "up to ten iterations … with less than 0.1 percent error, resulting in a total of 110 arithmetic operations"; (b) pdf p.37, P45 loop, "Eight iterations resulted in an error equal to less than 0.1 percent of the steady-state value"; (c) pdf p.38, the opened outer loop, "a restriction on time step to a maximum value of 10 msec was established based on a maximum allowable error of 0.1 percent between time steps". Only (c) sets Δt; it is a **relative, frame-to-frame** error, but the report never says on which variable (see the row on the 0.1% criterion, #24). Full derivation, equations and page citations in `body-realtime.md` §2 |
| 3 | Which trim conditions do the Appendix B linear models correspond to? | Phase 6 Jacobian validation | Table B.1, pdf p.67, read from the image | **closed** — three, all sea level, UH-60A 16825 lbm, NP 20895 rpm: hover (911.1 hp/engine, NG 41638, Wf 476.3 lb/hr); 80 kt level (552.6 hp, NG 39768, Wf 349.3); 80 kt + 1000 ft/min descent, γ = −7.08° (302.6 hp, NG 38072, Wf 267.7). Whether the nonlinear results sit at these same trims is still unchecked |
| 4 | `TC_T41` units are printed `lb_m^(4/5) · sec^(9/5) / deg R^(1/2)` (Table A.1, pdf p.55, and identically in the nomenclature, pdf p.12), but Eq. 51 (pdf p.26) requires `lb_m^(4/5) · sec^(1/5) / deg R^(1/2)`. Which is right? | Phase 3 heat-sink model; unit checking of TC_T41 | Glyph re-read at 600 dpi — it is unambiguously a `9`, not a `1`. So the report contradicts itself; this is a typesetting error, not a scan artefact | **closed 2026-09-12 — the printed units are the defect; Eq. 51 is right.** The question was which of the two the report means, and dimensional analysis settles it without appeal: Eq. 51 reads `M c_pm/(h A_m) = TC_T41 * sqrt(T41) / W41^(4/5)`, whose left side is a time, so `TC_T41` must carry `sec^(1/5)`. The printed `sec^(9/5)` cannot be made consistent with the equation that uses it at any value of the constant. The glyph is unambiguously a 9 at 600 dpi, so this is the report contradicting itself in print, not a scan artefact — the report's third such defect, after Fig. A3's displaced label (#42) and Fig. A10's inverted axis (#5). **The value 0.29 is unaffected and is what the model uses.** Separately, `TC_T41` is one of two candidates for the 7.3 % gap in `tau1/tau2` — that is #31 and is not this question. |
| 5 | Figure A10 (pdf p.65) labels its ordinate `PS9/P49`, values 1.02–1.14, but Eq. 38 (pdf p.24) needs `f₁₀ = P49/Ps9`. Is the axis label inverted? | Phase 2 exhaust pressure loss `f₁₀` | Axis label re-read at 600 dpi and Eq. 38 at 300 dpi; both are as stated |**closed — resolved by decision (user, 2026-09-10).** Digitize Figure A10 **exactly as printed** (`PS9/P49`, 1.02–1.14) so the data file stays faithful to the page; take the **reciprocal at the point of use** to form `f₁₀`. The CSV carries the printed quantity; `1/x` appears in code with this row cited beside it. Nothing is silently corrected on the way in **REOPENED AND RE-RESOLVED 2026-09-10, during implementation of Eq. 38.** The earlier decision (store as printed, take the reciprocal at use) was **wrong**. The values are f10 directly; only the axis LABEL is inverted. Physics: Eq. 37 sets Ps9 = P_amb, so P49 = P_amb·f10; expansion requires P45 > P49 > Ps9, hence f10 > 1; every plotted value is 1.021–1.124. Taking the reciprocal gives P49 = 0.89–0.98 × P_amb — exhaust total **below** ambient, which no running engine can do. Corroborated by the neighbouring maps: f8's P49/P45 spans 0.3007–0.8505 and f9's Ps9/P45 spans 0.3003–0.8500, so P49 ≈ Ps9 and their ratio sits just above 1. **Use the digitized values as they stand.** `src/t700/maps.py` and the CSV header corrected; `tests/test_maps.py` guards it.|
| 6 | `J_load` (Eq. 46, pdf p.25: `J = J_PT + J_load`) is not in Table A.1. | Phase 5 power turbine / load dynamics | Table A.1 (pdf p.55) gives `J_PT` only; searched the whole appendix | **partly closed** — `J_load` recovered, `dQreq/dNP` still missing. Original note: almost certainly an external Gen Hel quantity, like `Q_req`. Confirm it is a boundary-condition input rather than a missing constant **CLOSED 2026-09-11 — recovered from Appendix B, 0.186 % consistent across three flight conditions.** `j_load = 9.2503 * J_PT = 0.57352`, recorded as `constants.J_LOAD_UH60A`. Method: our NP row runs with `j_load = 0`, so for every element but the diagonal the ratio to Ballin's is purely the inertia ratio. `b(NP)` is the clean estimator because `b(NG)`, `b(P41)` and `b(P45)` all agree with Ballin to 0.1 %, so the fuel derivative is right and the factor of ten cannot be the fuel path. It gives 10.2503 / 10.2610 / 10.2417 at hover / level / descent. **The consistency is the proof**: inertia is the only quantity in that row that *must* be identical at all three conditions, and a units error or a wrong Q_PT model would not land within two parts in a thousand three times over. Feeding it back takes the NP row from ~900 % error to within 12 %, and `b(NP)` to 0.1 %. It is a property of the UH-60A airframe Appendix B was trimmed on, not a Table A.1 engine constant, so the model default stays 0 and it is opt-in. **Still open, separately:** the NP *diagonal* keeps a ~52 % residual at all three trims, which is dQreq/dNP — the other Gen Hel quantity, positive (load torque rises with NP), trim-dependent at roughly 1.78 / 1.45 / 1.18 times \ dQ_PT/dNP\ . |
| 7 | Figure A6 (pdf p.61) labels its abscissa `FUEL-TO-RATIO, FAR` — the word "AIR" is missing. | Cosmetic; affects nothing numerically | Re-read at 600 dpi; the label really is printed that way | closed — a typo in the report. Recorded so nobody re-reads the axis looking for a different variable |
| 8 | Appendix C prints no breakpoint tables for its eight schedule functions (F_EC1, F_HM1…F_HM7) — only plots, Figs. C23–C30. Are the numeric tables anywhere else in the report? | Phase 2 fuel control data capture; nothing in the HMU/ECU can run without them | pdf pp.77–101 read in full; no tables, only Figs. C23–C30 | **closed 2026-09-12 — genuinely absent, and seven of the eight are now digitized.** The tables are nowhere in the report; Appendix C gives these functions only as plots. `data/schedules/` holds F_HM1..F_HM6 (Figs. C24-C29) from the single-curve path and F_EC1 (Fig. C23) by curve tracing. **F_HM7 (Fig. C30) is not extracted** and is tracked on its own from here: its seven curves cross around PCNGHL 88-92, where curve 4 runs above 5, 6 and 7, so rank in y is not identity — only 70 of 410 sampled columns resolve seven separate runs, and those lie outside the bundle. It needs per-curve tracking through the crossings seeded by the printed digits, which is a different algorithm rather than a parameter. See `docs/notes/digitizing-recipe.md`. |
| 9 | `NGDES` (NG design speed) is used in Fig. C15 but never defined in Appendix C. Is it an Appendix A constant? | HMU NG spool sensor | pdf pp.77–84, then Table A.1 pdf p.55 | **closed** — yes. `NG_des = 44700.0 rpm`, Table A.1 row 31. (Companion `NP_des = 20900.0 rpm`, row 32, consistent with the Appendix B trim NP of 20895.) |
| 10 | Fig. C20 (p.93) gives `WFIDM = KNDRP·(PCNGI − PCNGHL) − WFIRF`. At idle this is ≈ −2.4, i.e. negative, so the `MAX(WFPDM, WFIDM)` idle floor appears never to bind. Is the printed sign right? | HMU idle limit | Fig. C20 re-rendered at 600 dpi and read directly; Figs. C28/C29 for ranges; body text pdf p.44 | **closed 2026-09-10 — the signs are correct and the earlier reading was wrong.** Fig. C20 is unambiguous: PCNGI enters `+`, PCNGHL enters `−`, the difference passes KNDRP = 0.25, and WFIRF enters `−`. The `≈ −2.4` figure is simply `−WFIRF` evaluated **at** the idle reference, where `PCNGI − PCNGHL ≈ 0`. That is the correct behaviour of a droop-line floor: it must not override the governor at or above idle. It goes positive once sensed NG droops below the idle reference by `WFIRF/KNDRP` ≈ 8.2–10.6 percentage points, and beats a 1.5–4 demand at ≈ 14–27 points below — i.e. deep sub-idle. And the report [pdf p.44] explicitly eliminates *"fuel control below flight-idle power"* from the model, so that regime is out of scope by design. The floor is inactive in every modelled condition **because the model stops above it**, not because of a sign error. Implement as printed. |
| 11 | Units clash in the Fig. C3 nonlinear NP-loop-gain circuit: the integrator differences TRQL (ft·lbf per the nomenclature, p.80) against CR = 20.0 "nondimensional" and limits the result to [YLOLIM, YHILIM] = [0.0, 40.0] "nondimensional". Same for the 180 threshold on TRQL in Fig. C8. Is TRQL normalised somewhere? | ECU governor rate compensation; sets whether B4 is ever 0 | pdf p.67 Table B.1, pp.77/80/81 Appendix C nomenclature, pp.83–84 Table C.1, Figs. C2 p.85, C3 p.86, C8 p.88 — all re-read at 400–600 dpi | **closed — our error, not the report's.** TRQL is not normalised and needs no normalisation: it really is ft·lbf, but it is *shaft-referenced* power-turbine torque, which is only 76–229 ft·lbf at the three trims — not thousands. Table B.1 (pdf p.67) prints both rows and the previous reading conflated them: "Required torque (ref. hub)" = 32865. / 20747. / 10792. ft·lbf is the **rotor hub** number (≈81:1 UH-60A main-gearbox reduction; 2 × shaft × 81 recovers it to within 8–12%, the tail rotor and accessory offtakes) and never enters the ECU, while "Engine torque (ref. shaft)" = 229.0 / 138.9 / 76.06 ft·lbf is what TORQ45 is. Arithmetic check: Q = 5252·hp/NP gives 5252·911.1/20895 = 229.01, 5252·552.6/20895 = 138.90, 5252·302.6/20895 = 76.06 — reproduces the printed row to 4 s.f. The signal path carries **unity DC gain end to end**: TORQ45 (= Q_PT, ft·lbf, p.80) → 1/(TL1·s+1) → 1/(TL2·s+1) → TRQL [Fig. C2, p.85] → straight into Figs. C3 and C8 with no gain, divide, 100/X or lookup anywhere (re-read at 600 dpi). So every constant lands sensibly in ft·lbf: CR = 20.0 ft·lbf ≡ 79.6 hp, a sub-idle "engine is loaded" threshold — and the Appendix C nomenclature p.77 calls CR exactly that, "engine torque-level threshold for nonlinear NP loop gain circuit"; the Fig. C8 threshold 180 ft·lbf ≡ 716 hp, sitting between the 80-kt trim (552.6 hp) and hover (911.1 hp), a proper "other engine at high power" discriminator. **B4 is not permanently pinned:** Y = ∫(TRQL − CR)dt clamped to [0.0, 40.0] crosses the CORR = 20.0 relay threshold in 0.10 s at hover, 0.17 s at 80 kt, 0.36 s at the descent trim, then holds B4 = 1; it unwinds to 0 in ≥ 2 s whenever TRQL < 20 ft·lbf (engine unloaded, autorotation), giving B4 = 0. That is precisely what ZK1 is for — p.81 defines it as "NP loop additional proportional gain for ECU governor rate compensation **used during high power operation**". Residual, recorded but not blocking: "nondimensional" is the **report's own word**, verified at 600 dpi in Table C.1 for CORR, CR, YHILIM and YLOLIM — not a previous agent's inference — and it is simply loose bookkeeping. Dimensionally CR is ft·lbf, Y is ft·lbf·s, and the summer ahead of the CORR relay adds the dimensionless 1000·B6 relay output to Y, so that node is inhomogeneous however it is labelled. Reproduce the numbers as printed and ignore the Table C.1 units column for these four constants. |
| 12 | Fig. C17 (p.91) feeds F_HM3 and F_HM4 with XLDSH (load demand spindle angle **with** hysteresis), but the x-axes of Figs. C26 and C27 are labelled XLDSA (**without** hysteresis). Which is the table's independent variable? | HMU load demand compensation | pdf pp.91, 97, 98 | **closed 2026-09-12 by decision, flagged — the block diagram governs, the axis label does not.** A lookup table's argument is whatever signal is fed to it, and Fig. C17 [pdf p.91] unambiguously feeds `XLDSH`, the post-hysteresis angle. The axis titles on Figs. C26 and C27 read `XLDSA`, which is the pre-hysteresis angle from the collective rigging of Fig. C13 (`XLDSA = 0.914*XCPC + 5.34`, then `XLDSH = hysteresis(XLDSA, XLDHYS)`). **Use `XLDSH`.** It is not cosmetic: `XLDHYS = 2.5 deg` on a schedule whose knots are 10 deg apart, so the two differ by a quarter of an interval at a reversal. Recorded as a naming slip in the axis title, the same class as Fig. A10's inverted label. Mark the Phase 5 call site `# UNVERIFIED-ARGUMENT` and cite this row; if a later reading shows the plot really was drawn against the pre-hysteresis angle, the fix is one substitution at that call site. |
| 13 | The saturation between the DWFPL/TMRU summer and the WFPDM summer in Fig. C9 (p.89) carries no limit labels, unlike every other saturation in Appendix C. Read from the icon as `max(·, 0)`. Confirmed anywhere? | HMU fuel-flow demand | pdf p.89, 600 dpi | **closed 2026-09-12 — `max(x, 0)`, and the architecture confirms the icon.** The icon itself is less ambiguous than 'icon read only' suggested: it shows a flat segment on the negative side and a unity-slope segment on the positive side, with no limit labels at all, which is `max` against a floor of zero and no ceiling. The report then corroborates it in words: the HMU **is the primary loop and works alone if the ECU trim is zero**, and the ECU's whole authority runs through `TMRU` [Appendix C, Fig. C1/C9 discussion]. In `WFPDM = WFPTP + KNDRP*(NGREF - PCNGHL) - WFPRF - max(DWFPL + TMRU, 0)`, dropping the saturation would let a negative `DWFPL + TMRU` **add** fuel above the HMU's own mechanical schedule — authority the report explicitly denies the ECU. The floor at zero is what makes the trim one-sided, and that is a structural argument, not a pixel one. |
| 14 | Eq. 43 (pdf p.25) prints `P41 = K_V41 ∫ T41(WA31 − W_f − W41) dt` — fuel flow enters the station-4.1 mass balance with a **minus**. A conservation-of-mass balance on that volume requires `WA31 + W_f − W41`. Is the printed sign a typesetting error? | Phase 3 control-volume dynamics; P41 state, and through it WA31 (Eq. 18) and the whole combustor loop | Both operators re-read at 600 dpi and pixel-zoomed, and compared against known `+` glyphs on the same page (Eq. 44 `+ B_3 K_bl WA2`, Eq. 46 `J_PT + J_load`), which show a clear vertical stroke these two do not. Both signs in Eq. 43 are unambiguously minus | **closed 2026-09-12 — superseded by #29, which the report settles.** This row said 'reproduce as printed' and #29 concluded the opposite; they contradicted each other and the code for some time. #29 is right: Eq. 73 [pdf p.36] prints the same continuity equation with `+ W_f`, and Eq. 77 follows from Eq. 73 rather than from Eq. 43. The report prints the balance both ways and **uses** the `+ W_f` form in the real-time solution, so the minus in Eq. 43 is a typesetting error and reproducing it would contradict two of the report's own equations. `src/t700/engine.py` implements `wa31 + wf_pps - w41` with the reason inline. The replication rule bends here because the choice is not ours: the report itself prints both and shows which it used. |
| 15 | `T_std` (Eq. 5, pdf p.22) and `P_std` (Eq. 8, pdf p.22) are in the nomenclature (pdf pp.11, 13) but **no value is printed for either anywhere in the report**. | Phase 1 — every corrected quantity: NG_c (Eq. 6), δ_2 (Eq. 8), WA2 (Eq. 9), and through NG_c the maps f₁, f₃, f₁₀, f_hs | Table A.1 (pdf p.55, all 33 rows), nomenclature pp.9–13, body pp.15–25 |**closed by decision** — these are standard-atmosphere reference values, but supplying them from a handbook violates the provenance rule. Search Appendix C and the real-time section before conceding, then flag the site `# UNVERIFIED:` and ask **Supporting evidence found 2026-09-10:** Table B.1 [pdf p.67] prints the sea-level trim ambient as `P2 = 14.696 PSIA` and `T2 = 518.67 deg R` — exactly the standard-day values. Since θ₂ = T2/T_std and δ₂ = P2/P_std must both equal 1.0 at sea level, this pins T_std = 518.67 deg R and P_std = 14.696 psia. Strong, but still an *inference*: the report never defines the two symbols. **DECIDED 2026-09-10 (user): adopt T_std = 518.67 deg R, P_std = 14.696 psia.** Implemented in `src/t700/corrections.py`, where they are labelled a decision rather than a transcription, with the reasoning in the module docstring and pinned by `tests/test_corrections.py`. Was blocking — it blocks every corrected quantity (NG_c, WA2_c, W41, W45).|
| 16 | `778.12` (ft·lb_f per Btu) appears as a bare inline number in Eqs. 39, 40 and 41 (pdf p.25) and is **not** a Table A.1 row and **not** a nomenclature entry. | Phase 5 torques — Q_C, Q_GT, Q_PT | Table A.1 pdf p.55; nomenclature pdf pp.9–13 | closed by transcription — it is printed three times in the equations themselves, so it *is* sourced. Cite `[TM-100991 pdf p.25, Eq. 39]`, not a handbook, and use 778.12 exactly (not 778.169 or 778.26) |
| 17 | Eq. 23 (pdf p.23) `T41 = T41_ns · f_s(T41, T41_ns, W41, NG_c)` is **implicit in T41**, and is further coupled to Eq. 28 (`W41 = K_WGT·P41/√θ_41`) through θ_41 (Eq. 25), which depends on T41. So T41 → θ_41 → W41 → f_s → T41 is a closed algebraic loop. How is it broken in the real-time implementation? | Phase 3 heat-sink model; Phase 4 integration scheme and step ordering | pp.22–25 read in full — the body states only that T41 is *"expressed as a transfer function with slowly varying coefficients"* (p.23) and defers to the following subsection (Eqs. 50–53, p.26) | **closed 2026-09-12 — the loop is opened with lagged coefficients, exactly as #2 guessed.** The implicit chain is T41 -> theta_41 -> W41 -> f_s -> T41. It is broken by evaluating the transfer function's coefficients at the **previous frame's** T41 and W41 rather than this frame's: Eq. 51 needs `sqrt(T41)` and `W41^(4/5)`, and `W41` is not computed until after T41 is known, so `realtime.RTState` carries both across the frame boundary. That is the same device Eq. 74 uses to open the compressor mass-flow iteration, and the report's own phrase **'a transfer function with slowly varying coefficients'** [pdf p.23] is the licence for it. With the heat sink off there is no loop at all: Eq. 23 degenerates to `T41 = T41_ns`. Implemented in `realtime._heat_sink` and exercised by `tests/test_heat_sink.py`. |
| 34 | Table 3's "Real-time model" rows were produced with **compressor-mass-flow and turbine-energy functions derived from the NASA-Lewis test engine, supplied by NASA Lewis** — explicitly *"in place of the standard functions which represent specification performance"* [pdf p.39]. Those substitute functions are not printed anywhere. | Phase 6 — deciding whether Table 3 (pdf p.44) is a validation target at all | pdf pp.39, 43, 44 read at 600 dpi; Appendix A pp.55–66 holds only the *standard* functions | **closed by the report itself — Table 3 is NOT reproducible** from a model built on Appendix A, and its caption says so ("This does not represent specification T700 performance"). Record Table 3 as evidence about method, never as a pass/fail target, and never tune a constant toward it |
| 35 | What power turbine speed / load condition were the steady-state sweeps of Figs. 6, 7 and 8 (pdf pp.40–42) run at? Shaft horsepower (Fig. 7) and Ps3 (Fig. 8) both depend on NP, but only "sea-level standard atmosphere conditions" is stated. | Phase 6 — reproducing Figs. 6–8, the three steady-state reference figures | captions and body text pdf pp.39–42, read at 300 and 600 dpi | open — a plausible reading is NP at design speed (NP_des = 20900 rpm, Table A.1 row 32), but that is **inference**. Try NP_des first and record the assumption at the point of use |
| 36 | Figs. 9 and 10 (pdf pp.45–46) were run with "the power turbine speeds … held constant by suppressing the NP integration" [pdf p.39]. **The held NP value is not printed**, nor are the ambient P2/T2 for those runs. | Phase 6 — reproducing the two open-loop transient references | pdf pp.39, 45, 46 | open — text says the dynamometer load was "used to trim the power turbine at the design speed for a specified fuel flow", which points at NP_des, but the figure and caption state neither NP nor ambient conditions |
| 37 | The fuel-flow step time in Figs. 9 and 10 is **read off the figure** as t ≈ 0.5 s (Fig. 9) and t ≈ 0.55 s (Fig. 10). It is stated nowhere in text or caption. | Phase 6 — time-aligning our transient against the digitized reference | pdf pp.39, 45, 46, 47 | open — low risk. Align on the step edge rather than on absolute t when comparing |
| 38 | `WFPH`, the ordinate of panel 1 of Figs. 9 and 10, is defined nowhere — not in the main nomenclature (pdf pp.9–13) nor the Appendix C nomenclature (pdf pp.77–82). | Reading Figs. 9/10; naming the plotted quantity | both nomenclature sections searched in full | **closed by inference, flagged** — it is fuel flow in lb_m/hr (the captions give the step as "400 to 775 lb_m per hour" and the panel values match). Inferred, not printed; do not treat as a distinct symbol |
| 39 | Figures 11–15 (pdf pp.48–53) all compare the **Gen Hel UH-60A blade-element helicopter simulation** carrying the T700 against flight test. Neither the helicopter model, its trim states, nor the flight-test time histories (refs. 13 and 14) are in this report. | Phase 6 scope — five of the report's ten result figures | pdf pp.47, 50 name refs. 13 and 14; the references list is pdf p.102 | **closed — out of scope.** Five result figures are unreproducible by construction, consistent with `SCOPE.md`'s existing Gen Hel boundary. State it in SCOPE.md rather than rediscovering it in Phase 6 |
| 40 | Figure 12's caption says the lateral cyclic input is "at high forward speed" (pdf p.49) but the body says "an airspeed of 55 knots" (pdf p.47). Fig. 11, the genuinely high-speed case, is 90 kt. | Cosmetic unless Fig. 12 is ever reproduced | both re-read at 600 dpi; both are as printed | **closed 2026-09-12 — cosmetic, and the figure is out of scope anyway.** A caption copied from Fig. 11, the genuinely high-speed case at 90 kt. The body's **55 kt** is the airspeed; both were re-read at 600 dpi and both are as printed. It can have no effect here because Fig. 12 is one of the five figures closed as unreproducible by construction in #39 — it needs the Gen Hel UH-60A blade-element simulation, which this report consumes and does not contain. Recorded so nobody re-reads the caption looking for a second high-speed case. |
| 41 | pdf p.38 prints "…which was provided by **GF** and NASA Lewis." The second glyph has no lower bar and no lower serif at 1200 dpi — it is an F, not a damaged E. | Cosmetic; affects nothing numerically | re-rendered at 1200 dpi and inspected pixel-by-pixel | closed — a typo in the report for "GE". Recorded so nobody hunts for an organisation called GF |
| 22 | Eq. 74 (pdf p.36) prints `WA31_(n) ≈ WA3_(n) − WA3_bl(n−1)` — only the diffuser bleed is lagged — but the sentence immediately above it says the combustor inlet flow equals "the mass flow leaving the compressor **in the previous interval**", i.e. `WA3(n−1) − WA3_bl(n−1)`. Which is the implementation? | Phase 4 — this *is* the opened iteration; the choice sets what carries across frames | Eq. 74 re-read three times from independent 600-dpi crops, and cross-checked against the OCR text layer, which independently gives `(n)` on `WA3` | **closed 2026-09-11** — settled by a convergence test; see the CLOSED note below. Original note: reproduce **as printed** and record the objection. Note the physics argument favours the prose: lagging only `WA3_bl` would not open the loop at all, because `WA3(n)` still depends on `P3(n)` through Eqs. 4/7/9/17 (pdf pp.22–23). Worth testing both and comparing the time-step sensitivity against pdf p.38's 10 ms / 0.1% **CLOSED 2026-09-11 — the PROSE reading is right; the equation as printed is not a consistent discretization.** Decided by the report's own criterion rather than by preference. Implementing both and refining the time step on a 400→775 lbm/hr fuel step, NG at t = 1.0 s: the prose reading (whole compressor exit flow lagged) gives 43955.0 → 43860.8 → 43821.5 → 43804.8 → 43796.9 → 43793.1 rpm at dt = 8, 4, 2, 1, 0.5, 0.25 ms — successive differences 94.1, 39.4, 16.7, 7.9, 3.8, i.e. **halving dt halves the error: clean first-order convergence to a limit.** The printed reading (bleed only lagged) gives 42348.4 → 42363.1 → 42456.0 → 42585.3 → 42763.6 → **43959.0** — differences −14.7, −92.9, −129.3, −178.3, −1195.4, growing as dt shrinks and then jumping. It converges to nothing. A scheme that does not converge as dt → 0 is not a discretization of any continuous system. Corroborated physically: the printed reading peaks T41 at **9991 °R** on that step against 3114 °R for the prose reading, and it cannot open the loop anyway since WA3_(n) still depends on P3_(n). `lag_whole_flow=True` is the default in `src/t700/realtime.py`; the printed reading is retained behind the flag so the finding stays reproducible. |
| 23 | How is the cross-frame compressor mass flow of Eq. 74 initialized at t = 0? | Phase 4 — first-frame behaviour and determinism | pdf pp.36–38 and p.47 | open — the only nearby statement is pdf p.37–38, "An equilibrium solution may be obtained within a specified error tolerance", which implies a trim solve could seed it. No initialization rule is printed. Seeding from the converged trim is the obvious choice; mark the site `# UNVERIFIED` |
| 24 | The 0.1 percent "maximum allowable error between time steps" that caps Δt at 10 ms (pdf p.38) — measured on **which variable**, relative to what, under what transient? | Phase 4 — turning the report's own criterion into a test | pdf p.38, and pdf p.37 where the two *other* 0.1% figures are defined | **closed 2026-09-12** — answered from pdf p.38. Original note: the p.37 figures are explicit about their referents (one is "of the steady-state value"), the p.38 one is not. Without it the 10 ms cap can be reproduced as a number but not as a criterion  **ANSWERED 2026-09-12 from pdf p.38, read off the raster.** The sentence is: *"Because of the opened compressor mass-flow iteration, however, response is dependent on time step. For the T700 implementation, a restriction on time step to a maximum value of 10 msec was established based on a maximum allowable error of 0.1 percent between time steps."* So the referent is the **response, frame to frame** — not a steady-state value — and the report names the mechanism: the opened compressor mass-flow iteration of Eq. 74. Ballin is stating outright that his transient response depends on dt, which we then measured: our T41ns overshoot runs 385.0 / 384.9 / 397.5 / 407.1 / 412.6 / 417.0 degR at dt = 14 / 7 / 3.5 / 1.75 / 0.875 / 0.4375 ms. Closed as answered; the criterion is now reproducible as a criterion and not merely as the number 10. |
| 25 | How many iterations does the shipped real-time implementation actually run for the inner P3/P41 loop and for the P45 loop, and against what stopping test? | Phase 4 — frame cost and determinism | pdf pp.35–37 | **partly closed 2026-09-12** — the tolerance and caps are printed and now implemented; the pass count is not. Original note: "up to ten iterations" (P3/P41) and "Eight iterations" (P45) are **worst-case measurements under extreme step inputs**, not a stated policy. pdf p.35 records that Ames "require[s] a minimum amount of multiple-pass coding or iteration", which pushes toward a fixed count, but no count and no convergence test is printed  **HALF PRINTED AND NOW IMPLEMENTED, half still open — and shown to be load-bearing, 2026-09-12.** This row was filed as unknowable bookkeeping. It is not: it materially changes every transient. What IS printed on pdf p.37, and was being ignored: the P3/P41 sweep converges *"with less than 0.1 percent error"* in *"up to ten iterations ... resulting in a total of 110 arithmetic operations"* (eleven operations a pass x ten passes, which pins the cap independently of the sentence), and for P45 *"Eight iterations resulted in an error equal to less than 0.1 percent of the steady-state value"*. **Our loops ran to `tol=1e-10` with a cap of 40 — seven orders of magnitude tighter than the report, and invented.** Now `realtime.TOL_PRESSURE = 1e-3`, `MAX_ITER_P3_P41 = 10`, `MAX_ITER_P45 = 8`, each cited. Converging harder than Ballin did makes the pressures reach equilibrium inside one frame where his relaxed toward it across several, which sharpens every transient and — on a fuel cut — lets the engine plunge deeper. Measured effect: Figure 10's NGc minimum 70.94 -> **74.64 %** against Ballin's 74.2 %, settled T41 +11.5 -> **+3.2 %**, settled T45 +17.4 -> **+3.4 %**; Figure 9's T41 peak +4.36 -> +3.98 % and its overshoot ratio 1.83 -> 1.73x. Steady state is untouched, as it must be — a fixed point is a fixed point — and a trimmed engine still sits still to 1.9e-16. **What remains open**: the report never says how many passes were actually *taken* per frame, only how many *may be required*. That is worth another 40 degR — a fixed single pass gives the Figure 9 overshoot as +145.9 degR against convergence-to-0.1%'s +184.9 and Ballin's +116.3, and moves the peak from t+14 ms to t+21 ms against his t+30 ms. Not adopted, because a fixed count is not printed and choosing one to fit the figure would be tuning. |
| 26 | **The report never names a numerical integration algorithm.** No Euler, Runge-Kutta, trapezoidal, Adams or predictor-corrector appears anywhere. | Phase 4 — the core integrator | Text layer of all 104 pages grepped for `euler\|runge\|trapezoid\|adams\|predictor\|corrector\|solver`; the only "integrat*" hits in the body are "suppressing all integrations" (p.28), "suppressing the NP integration" (p.39) and the Appendix C control-law integrators | open — states are printed in integral form (Eqs. 42–47, pdf p.25) and in the real-time model only NG and NP (and T41 with the heat sink) are integrated at all, the three pressures being solved algebraically. `CLAUDE.md` already mandates fixed-step explicit integration; the specific method is our choice, made in the open, not the report's|
| 27 | Numeric values of the relaxation parameter `R` (Eq. 71, pdf p.36) and the Lipschitz constant `L` (Eq. 70) are not given. | Phase 4 — convergence behaviour of the fixed-point solves | pdf pp.36–37, Table A.1 pdf p.55 | open — pdf p.36 says only "A value of `R` was chosen to allow monotonic convergence over the operating range of the engine", and pdf p.37 says the P3/P41 loop needs **no** relaxation at all. Where `R` is applied is also unstated |
| 28 | Eq. 71 (pdf p.36) is printed `x_(n) = x_(n−1) + R · [ f(x_(n)) − x_(n−1) ]` — with `x_(n)` on both sides, making it implicit as printed. Standard successive overrelaxation would use `f(x_(n−1))`. | Phase 4 — only if `R` is ever applied | Re-read at 600 dpi; the subscript really is `(n)` | **closed 2026-09-12 — moot: `R` is never applied.** The relaxation of Eq. 71 matters only for the fixed-point loop of Eqs. 76/78, and the report states that loop needs none — 'no relaxation algorithm is required for convergence' [pdf p.37]. Our implementation agrees empirically: the inner P3/P41 sweep converges at every trim and through every transient in `validation/` without relaxation, and `R` appears nowhere in `src/t700/` outside that quoted sentence. So the printed form being implicit in `x_(n)` never has to be resolved. **If `R` is ever needed, use the standard explicit form with `f(x_(n-1))` and cite this row** — the printed version cannot be evaluated as written. |
| 29 | **Resolves the Eq. 43 fuel-sign question raised earlier in this table** (the row asking whether the `− W_f` in Eq. 43, pdf p.25, is a typesetting error). | Phase 3 / Phase 4 — combustor mass balance | Eq. 73 (pdf p.36) and Eq. 77 (pdf p.37) re-read at 600 dpi against Eq. 43 (pdf p.25) | **closed — the report answers itself.** Eq. 73 prints the same continuity equation as Eq. 43 but with `+ W_f`: `Ṗ41 = 0 = K_V41 · T41 · (WA31 + W_f − W41)`. Eq. 77 then states `WA31 = W41 − W_f`, which follows from Eq. 73 and **not** from Eq. 43. So the report prints the balance both ways and uses the `+ W_f` form in the real-time solution. Implement `+ W_f`; the `− W_f` in Eq. 43 is a typesetting error, and reproducing it as printed would contradict Eqs. 73 and 77 |
| 30 | Eqs. 79 and 80 (pdf p.37) print `B₃ B₄ WA2`, but **`B₄` is not in the nomenclature** (only B₁, B₂, B₃ are), and Eq. 44 (pdf p.25) writes the identical physical term as `B₃ K_bl WA2`. | Phase 3 — turbine cooling flow reintroduced at station 4.5 | `B₄` re-read at 600 dpi, unambiguous; nomenclature pdf pp.9–13 checked | **closed 2026-09-12 — `K_bl`, decided, implemented and empirically supported.** Three independent reasons. `B4` appears in no nomenclature [pdf pp.9-13] while `K_bl` is defined (pdf p.10, 'fraction of diffuser bleed gas which is used to cool the gas generator turbine blades') and has a Table A.1 value. Eq. 44 [pdf p.25] writes the identical physical term as `B3 K_bl WA2`. And the model built on that reading reproduces Table B.1 to 0.014 / 0.132 / 0.026 % on NG — the term feeds the station-4.5 mass balance directly, so a wrong constant there would not pass unnoticed. Used in `engine.py` (Eq. 44) and `realtime.py` (Eq. 79) with the substitution noted at both sites. |
| 31 | Numeric values of the heat-sink lead/lag time constants `τ₁` and `τ₂` (Eq. 63, pdf p.31) are not printed anywhere. | Phase 6 — reproducing the 3- and 6-DOF linear models | pdf pp.28–34, Appendix A Table A.1 pdf p.55, Appendix B pdf pp.67–76 | **partly closed, and blocked by the source.** Original note: they are trim-dependent by definition ("those values corresponding to the trim operating condition", pdf p.31) and must come from our own Eqs. 50–53 evaluated at trim. `A(3,3)` of the 3-DOF models is **not** simply `−1/τ₂`; it is the (3,3) element of `F₁⁻¹F₂` **PARTIALLY RECOVERED 2026-09-11 from Appendix B's 6-DOF T41 row.** That row is linear in `(1/tau2, tau1/tau2)`, but the two are not equally determined. **`tau1/tau2` is well determined**: 0.6845 / 0.6571 / 0.6270 at the three trims, the six row elements agreeing to 0.74 % at hover, and an independent estimator — the feedthrough `d` — giving 0.6712 / 0.6450 / 0.6182. **`tau2` alone is NOT recoverable**: its independent signature in that row is worth ~18 in elements of ~198400, below the ±50 that 4-significant-figure printing allows, so a naive least squares returns *negative* time constants at all three trims. That is how the limit was found. Ours is 0.6378 / 0.6084 / 0.5810, uniformly **7.3 % low** (0.932 / 0.926 / 0.927) — the uniformity being the evidence, since a structural error would not be constant across three conditions. Equivalently our `tau_b/tau_a` is ~14 % high, and since `tau_b/tau_a = f_hs(NGc)/(TC_T41 * sqrt(T41) * W41^0.2)` the candidates are `f_hs` (digitized) and `TC_T41 = 0.29` (transcribed). **They cannot be separated here** — all three trims sit on `f_hs`'s flat top (7.497, spanning 85–100 % NGc) so it is indistinguishable from a constant; separating them needs a trim below 85 % NGc and the report gives none. See `docs/notes/derivative-ambiguity.md`. **DEGENERACY TESTED AND NOT BREAKABLE, 2026-09-11.** Figure 10 looked like the escape from the f_hs/TC_T41 degeneracy: its step-down drives NGc to 71 %, where `f_hs` has fallen to 4.35 against the plateau's 7.497, so the two hypotheses differ there by a factor of 1.73. Run with the heat sink active, they do **not** separate — T41 excursion -268.5 (f_hs scaled) against -261.5 (TC_T41 scaled), about 1.5 % apart and far below our own 11 % error on that panel. The reason is structural: the step fires at NGc 91 %, the fast response happens while still on the plateau, and NGc only reaches 71 % well after T41 has settled, so Figure 10 never exercises the region that would distinguish them. No trim or transient in the report sits below 85 % NGc while the heat sink is doing anything. **Blocked by the source, not by the replication.**  **THE CONFLICT WITH FIGURES 9/10 IS RESOLVED IN THIS ROW'S FAVOUR, 2026-09-12, and the f_hs hypothesis is falsified by its own figure.** Eq. 63 [pdf p.31] is `T41/T41ns = (tau1 s + 1)/(tau2 s + 1)`; Eq. 50 [pdf p.26] is `((Mc_pm/hA_m - Mc_pm/W_g c_pg)s + 1)/((Mc_pm/hA_m)s + 1)`. Matching them gives **tau1 = tau_a - tau_b and tau2 = tau_a**, so `tau1/tau2` IS the high-frequency gain `k = 1 - tau_b/tau_a` exactly, and p.31 states the constants are *'those values corresponding to the trim operating condition'* — the same formulas, evaluated the same way. So this row and Figures 9/10 constrain one quantity. They disagreed: this row gives 0.6845 / 0.6571 / 0.6270 against our 0.6378 / 0.6084 / 0.5810 (7.3 % low), while a multiplier scan on `f_hs` scored against the two transients wanted k about 1.7x SMALLER. **Three things settle it for this row.** (a) *The conditions are the same.* Appendix B's trims are 476.3 / 349.3 / 267.7 lb/hr, i.e. NGc 93.2 / 89.0 / 85.2; Figures 9/10 start at 400 lb/hr, NGc ~91. All of them sit at or above 85 % NGc, where `f_hs` is on its 7.485 plateau, so `f_hs` is *identical* at every one and cannot be the difference. (b) *Figure 9 never leaves the plateau* (NGc 91 -> 99.4), so its preference for x1.65 is a claim about the plateau value alone — which would put it at **12.35 on an axis that stops at 8.00**, with the plateau plainly drawn at 7.48. The factor is not in `f_hs`. (c) *The scan is degenerate and this row is not.* `T41 = k*T41ns + lag`, so a T41 excursion error is equally explained by k or by T41ns, and the scan only turns k. Appendix B constrains k directly. **Conclusion: k is right to 7.3 %, and the transient error is in T41ns, not in the heat sink.** See #47 for where it went. **REOPENED AS BREAKABLE 2026-09-12 — and Figure 10 is the dataset that breaks it.** This row concluded the `f_hs`/`TC_T41` degeneracy could not be separated because all three Appendix B trims sit on `f_hs`'s flat top (7.485, spanning 85-100 % NGc), and a 2026-09-11 attempt on Figure 10 found no separation. That attempt is superseded twice over. (a) It ran on the collapsed-lead-lag realization, whose stale-memory artifact (#47) was larger than the effect being sought. (b) The gas path is now verified exact at matched NGc — Ps3 to 0.8 %, the `f1` flow to 0.3 % — so a T41 residual can no longer be blamed upstream. **Figure 10's decay is the only dataset in the report that leaves the plateau**: it carries NGc from 91 % down to 75 %, where `f_hs` has fallen to ~5.9 against the plateau's 7.485. Measured there, our `tau_b/tau_a` falls from 0.4219 to 0.3851 (a factor 0.913) while his falls from ~0.4643 to ~0.3155 (a factor 0.680). His descent is steeper than `f_hs`'s own 0.791, which means it cannot be `f_hs` alone — the `sqrt(T41)/W41^(1/5)` dependence of Eq. 51 carries part of it and is the other half of the degeneracy this row records. That is the next thing to settle, and unlike before it is now a measurement against clean data rather than against an artifact.|
| 32 | The symbol `F` in `b̄ = F₁⁻¹G₁ + F F₁⁻¹G₂` (pdf p.33) is defined nowhere in the report and is not in the nomenclature. `F₁`, `F₂`, `G₁`, `G₂` all are. | Phase 6 — reproducing `b̄` for the 3-/6-DOF models | Re-read at 600 dpi: an upright italic `F`, no subscript. Nomenclature pdf pp.9–13 checked | **closed 2026-09-11 by derivation.** Original note: the standard Chen transformation for `ẋ = Ax + B₀u + B₁u̇` gives `b̄ = B₀ + A·B₁`, which would make `F ≡ A = F₁⁻¹F₂`. Transcribed as printed; do not assume. (Raised in `inventory-appendix-b.md` §7 item 3, recorded here as instructed) **ANSWERED 2026-09-11 by derivation: `F == A == F1^-1 F2`.** Implementing the descriptor form (Eq. 65) and the Chen transformation (Eqs. 66-67) directly: with `B0 = F1^-1 G1` and `B1 = F1^-1 G2`, the substitution `z = x - B1 Wf` gives `zdot = A z + (B0 + A B1) Wf` and `x = z + B1 Wf`. So `b = B0 + A B1`, matching the printed `b = F1^-1 G1 + F F1^-1 G2` only if `F` is `A`. Corroborated by two printed features the derivation reproduces without being told to: `C = [I]` in all six heat-sink figures, and `d` nonzero only in the T41 row. **Derived, not transcribed** — if a later reading finds `F` defined otherwise, this is wrong. |
| 33 | pdf p.38 caps the time step at **10 msec**, but pdf p.47 reports the actual configuration as a **14 msec** host rotor frame with the engine updated **every 7 msec** and the **NP degree of freedom updated every 14 msec** — i.e. NP runs at 14 ms, above the cap. | Phase 4 — frame structure; the model is multirate, 2:1 | pdf p.38 and pdf p.47, both read from page images | open — consistent reading is that the 10 ms cap applies to the *opened compressor mass-flow iteration* (which lives in the 7 ms engine update), not to the NP integration, which is deliberately tied to the drive-train/rotor-hub frame. Reproduce the 2:1 multirate structure as printed and record it |
| 42 | Figure A3 (pdf p.58): does the upper plateau of `f3` sit at 0.110 or 0.109? The printed `0.11` y-axis tick **label** sits ~19 px below its own tick mark — the only label on the page that does — and the plateau lines up with the displaced label, which makes 0.110 look right by eye. | Phase 3 seal-pressurization bleed `f3` | Tick lattice measured directly off the rectified render, independently of the digitizing agent | **closed 2026-09-10 — the plateau is 0.1091, and the LABEL is the defect, not the tick.** Decisive evidence: the 15-tick lattice is regular at 168.39 ± 1.97 px, and the 0.11 tick at y=483.5 fits it exactly (169.0 above, 169.5 below), so the tick cannot be the displaced element. Independent confirmation: the same linear fit maps the 0.00 tick to −0.000009, i.e. the lower plateau of `f3` lands on exactly zero. Upper plateau measures **0.10907**, sitting 17.0 px = 0.00101 below the 0.11 tick. This is the report's **third** typesetting defect, after the `TC_T41` exponent (#4) and the Fig. A10 axis inversion (#5). |
| 43 | What interpolation does the report use between the breakpoints of its function tables? Body text calls them "function table lookups" [pdf p.37] but never states the scheme, and spline vs linear changes every value *between* knots. | Phase 2 `t700.maps`; affects every f-function and therefore every result | Grepped body pp.15-54 and Appendices A and C for interpolat*/breakpoint/lookup; only the phrase "function table lookup" appears, with no scheme | **partly closed** — mechanism established, no cure. Original note: implemented as linear, by decision.** Two reasons: the figures are drawn as discrete knots joined by *straight segments*, which is what a linear table looks like when plotted; and a 1988 real-time model on a 10 ms frame would not have afforded spline evaluation. Recorded as a decision in `src/t700/maps.py`, not as a transcription. If a later reading finds the scheme stated, revisit — every value between knots moves. **EVIDENCE FOUND 2026-09-11, still open — Appendix B says linear is wrong, and does not say what is right.** The element-wise comparison isolates four derivatives — d(NG)/d(P45), d(P45)/d(P41), d(P45)/d(P45), d(NG)/d(P41) — that fail worst at hover and least at descent: -24.3 / -14.5 / -7.3 % on the first. All four run through `f7` [Eq. 26], whose **six** printed knots [pdf p.62, Fig. A7] make its interpolated derivative a step function: -526.4, -254.7, -150.8, -156.8, -108.1. The three trims land 98.6 %, 71.7 % and 32.5 % across their segments, and the error tracks the segment slope. The six knots are **not** a digitization failure: the digitizer verified the count two independent ways and the abscissae land on a 0.005 lattice. So the knots are Ballin's and the *interpolation between them* is ours. **Controlled test:** swapping `f7` alone to a shape-preserving cubic (PCHIP) moved exactly those four elements and left every other element unchanged — d(NG)/d(P3) stayed at +4.0 / +1.9 / +4.0 % in both runs. That pins the cause. It does **not** settle the cure: PCHIP is nearly exact at level (-14.5 % -> +0.6 %) but overshoots at hover (-24.3 % -> +22.5 %) and worsens descent (-7.3 % -> -12.2 %). The implied true slopes are about -423 at hover (between linear's -526 and PCHIP's -330), ~-222 at level (PCHIP is -225), and ~-146 at descent (below both). Trims barely move either way, so this is a derivative-only question. Do not change `maps.Curve` on this evidence; a third scheme is needed, or the difference is in `f7`'s own reading near the knee. **MECHANISM ESTABLISHED 2026-09-11 — it is knot proximity, and no scheme settles it. See `docs/notes/derivative-ambiguity.md`.** A second, independent error family confirms the first: the `d(.)/d(NG)` derivatives run through `f1`, which interpolates linearly *between speed lines* (65, 80, 82, 85, 87, 89, 92, 94, 96, 98, 100), so d/dNGc is a staircase there too. Level sits **2.8 %** past the 89 line and descent **7.5 %** past the 85 line, while hover sits **58 %** across [92, 94] — and the errors follow exactly: hover +0.6 / -0.5 / -0.1 %, level -7.5 / +5.2 / +9.5 %, descent +11.2 / -12.2 / -9.6 %. Signs check too: at level we use a segment 7.6 % steeper than the one below and `\ A(1,1)\ ` comes out 7.5 % too large; at descent a segment 27.1 % shallower and it comes out 11.2 % too small. **Hover is the decisive case** — 0.0002 from `f7`'s knot at 0.21496 AND mid-segment on `f1`, so it holds simultaneously the worst `f7`-driven errors and the best `f1`-driven errors in the whole comparison. One operating point, opposite outcomes, two tables. A wrong equation cannot do that. **Two candidate fixes both fail:** a shape-preserving cubic on `f7` is near-exact at level (+0.6 %) but overshoots hover (+22.5 %) and worsens descent (-12.2 %); and a larger extraction perturbation — which the report does use [pdf p.31], step size unstated — collapses hover's rms from 12.8 to **1.3** at 125 rpm (exactly the straddle of that `f7` knot) but the best step differs per trim (3e-3 / 1e-2 / 2e-2) because the knot distances differ, so one step cannot be the answer and tuning three would be fitting. **Do not change `maps.Curve`:** the evidence identifies the mechanism, not the scheme, and changing it moves every value between knots everywhere to fix derivatives that are ambiguous by construction. Corroboration that this is interpolation and not physics: trim *values* match Table B.1 to 0.13 % NG and 0.05 % shp while *derivatives* run 5-25 % out.  **f1 AUDITED AND CLOSED AS A SUSPECT, 2026-09-12.** The Figure 10 decay-rate error correlated so cleanly with `f1`'s 15-point [65, 80] speed-line hole that it looked like the mechanism. It is not. Four checks. (a) *The axes are not inverted and there is no unit error.* Figure A1 read at 300 dpi: y is STATION 2 CORRECTED MASS FLOW, WA2C, LBM/SEC, 2 to 12; x is COMPRESSOR STATIC PRESSURE RATIO, PS3/P2, 0 to 20. Our digitized values and per-line shapes match it. (b) *We use the map in its single-valued direction.* `P3 -> Ps3 = 0.956 P3` (Eq. 4) `-> PR -> WA2c = f1(PR, NGc)` (Eq. 7). Reading flow from pressure ratio is single-valued; the inverse would not be, which is why a beta parameterisation is normally needed and is not needed here. (c) **Figure A1 prints its beta lines** — six dotted diagonals threading the markers of all eleven speed lines — and our digitized points sit on them: every line has exactly seven corresponding points, a flat anchor at the left frame plus one marker per dotted line. Interpolating at constant Ps3/P2 therefore blends non-corresponding points, and at NGc 75 %, PR 4 it blends the 80 % line's choked plateau with the 65 % line clamped past the end of its own data. Beta interpolation was implemented and measured: Figure 10 slope ratio 1.524 -> 1.468, Figure 8 worst 1.63 -> 1.18 %, trim NG worst 0.131 -> 0.120 %, but shaft power worst 0.772 -> 0.912 % and Figure 6 worst 2.232 -> 2.301 %. **A wash** — real, principled, and far too small to be the 50 % effect being chased. Not shipped, because the evidence does not demand it; the structural argument for it stands on its own and is a decision to take deliberately. (d) *A shape-preserving cubic across speed lines makes it worse* (1.52 -> 1.86). `f7` was audited alongside and is also eliminated: Figure A7 read at 300 dpi confirms only six printed markers, five crowded below 0.231 and one at 0.350 joined by a straight line, so over the whole Figure 10 decay (P45/P41 0.2405-0.2743) we and Ballin read the same straight segment and linear is exactly right there. **`f1` EXONERATED INDEPENDENTLY, 2026-09-12.** The [65, 80] speed-line hole was the leading suspect for the Figure 10 decay. It is not the cause: Ballin's own W41 is recoverable from his plotted Wf, Ps3 and T41 through Eqs. 25, 28 and 78 with no map in the loop, and our WA31 matches it to +/-0.8 % through the whole hole, down to 77 %NG. Since `WA31 = WA2(1 - B1 - B2 - B3 - K_B3)`, `f1` and the bleeds are pinned jointly to that precision, and the bleeds are separately confirmed (see #47). Interpolation inside the hole is therefore right to better than 1 % in flow, whatever it does to derivatives. **THE Ps3 MECHANISM IS FOUND, AND IT IS AN UNDER-DETERMINATION RATHER THAN A BUG — 2026-09-12.** Walking Eq. 76 backwards at matched NGc on the Figure 10 chop gives the whole chain, and the amplifier is the **squaring in Eq. 18**: at 76 %NGc our WA31 is **+1.48 %** high, which Eq. 18 squares to **+2.99 %**, plus T3 at +0.58 % makes `P3(P3-P41)` **+3.68 %** high, which comes out as Ps3 **+2.17 %** high, which Ps3's own ~13x gain turns into the **-29 %** deceleration error. Two multiplications, one reading error. The root of the WA31 error is this row's own subject: at NGc 76 the bracket is [65, 80] and the **65 % line is asked for Ps3/P2 = 4.85 against a range that ends at 3.753 — 29 % beyond — and is clamped at its near-surge value 2.9651**, which is then blended with the 80 % line still sitting on its choked plateau. **But no scheme fixes it.** Four defensible readings of the hole, scored on Ps3 parity at 76 %NGc: constant-PR **+2.17 %**, beta **+3.17 %**, shape-preserving cubic **-3.02 %**, per-line linear extrapolation instead of clamping **-2.28 %**. The scheme choice alone spans **6 % of Ps3** — which at 13x gain is about 80 % of `dNG/dt`, larger than the discrepancy being explained. So Figure A1's fifteen-point hole makes Ps3 undeterminable at the 1 % level that `dNG/dt` needs, whichever defensible scheme is used. That is the answer to 'find the mechanism and you find the bug': the mechanism is found and there is no bug to find.|
| 44 | Digitized `f3` (seal-pressurization bleed fraction, Fig. A3) dips very slightly **negative** where the bleed closes — most negative knot −0.000104 at NGc 89.02, giving B1 = −0.0001 at the hover trim. A bleed fraction cannot be negative. | Phase 3 bleed accounting | Values are the digitized marker centroids; the excursion is smaller than the figure's read error and the lower plateau calibrates to −0.000009 on the 0.00 tick | **closed 2026-09-12 — the decision is implemented and verified, and a second extraction reproduces the dip.** The architecture does exactly what this row asked for: the CSV keeps the digitized value and the constraint is applied at load. `maps.Physics(nonnegative=True)` on `f3` clamps at zero in `condition()`, and `CONDITIONING` records the largest move, so the provenance chain back to the page is unbroken while nothing downstream ever sees a negative bleed. Measured: the file's minimum is **-0.000168** and the loaded curve's is **0.0**. The 2026-09-12 re-digitization, on a different frame fit and calibration, independently returns the same small negative excursion, which confirms it is what the plot shows rather than a one-off artefact of the earlier extraction — and it stays read error around a true zero either way. **Do not edit `data/`. **Extended 2026-09-12 to every table**, on the principle that these are hand-drawn 1988 plots and we know the physics behind them: `f1`, the one map that had no declared constraint, now carries one too — corrected flow is non-negative and falls along a speed line — moving the digitized map by at most 8.5e-4 lbm/sec on two of 66 segments and changing no trim. All 11 tables are now declared. The cross-speed-line ordering is asserted by `tests/test_maps.py` rather than conditioned, because the data already satisfies it and a constraint that never binds should be a test, not a filter.** |
| 45 | An open-loop fuel step of 400→775 lbm/hr drives the model well outside its digitized data: FAR reaches **0.0327** against `f6`'s plotted range of 0.010–0.020, and `f9` is evaluated below its 0.30 abscissa limit. Over a 3 s / 429-frame run the clamp counter records f9 920 times, f6 358 times, `f1` on three speed lines, `f7` once. | Phase 4 transients; the Figs. 9–10 comparison | `maps.clamp_report()` during `realtime.run` | **open — re-measured 2026-09-12, and it is a Phase 5 dependency rather than a defect.** The excursion is essentially unchanged by the re-digitization and by the heat sink: over the Figure 9 step, FAR still reaches **0.0327** against `f6`'s tabulated 0.00999-0.020003, and `f9` is still evaluated below its 0.3005 abscissa limit. Clamp counts over 430 frames are f9 914, f6 353, f7 1 and `f1` on three speed lines with the heat sink off; 854 / 353 / 1 with it on. What the heat sink does change is the peak it produces: T41 max falls from **3111 to 2902 degR**. Probably faithful — a 1988 function-table processor would clamp at the table ends too, and the printed axis range *is* the table's range — but it must be reported with any transient result, never silently extrapolated, and `validation/test_fuel_step.py` asserts the clamping rather than ignoring it. **This cannot close until the fuel control is closed around the engine**: WFPAC, the acceleration limit of Fig. C21, exists precisely to prevent an unrestrained step like this one, and that schedule is #50.  **CLAMPS MEASURED AND NOT LOAD-BEARING, 2026-09-12.** This row was the last standing candidate for #47, and it is now eliminated as a *cause* while remaining a faithfulness concern. Instrumenting every map call through the Figure 9 step: `f6` is asked for FAR up to 0.03267, **126.5 % beyond its tabulated range**, on 138 calls; `f9` 209 calls but only 1.2 % beyond; `f7` one call 6.0 % beyond; `f2`, `f3`, `f4`, `f5`, `f8`, `f10`, `f_hs` and `f1` never leave their ranges at all. Then relaxing each clamp to linear extrapolation, one table at a time, and measuring the T41 peak: `f6` **-0.2 degR**, `f7` -7.1, `f9` 0.0, `f8` 0.0, `f2` 0.0 — against a 112 degR gap to Ballin. The reason is structural and worth recording: `f6` is a **two-point, nearly constant table** (0.98504 at FAR 0.00999, 0.98496 at 0.02000), so clamping a combustor efficiency that barely varies costs nothing however far outside the range you go; and `f9` is **flat at its clamped end** (0.37219 at both of its first two knots, the choked plateau), so the clamp there returns what extrapolation would. The excursion is real and must keep being reported, but it does not hold the T41 spike up. **RE-MEASURED 2026-09-12 after adopting the printed convergence criterion (#25).** Figure 9's excursion is essentially unchanged — FAR still reaches 0.03267, `f6` clamps 638 times and `f9` 1103. **Figure 10's is much reduced**: NGc now bottoms at 74.64 % instead of 70.94 %, so `f1@65` clamps fall 510 -> 435 and the deep-plunge argument for that figure's disagreement is largely withdrawn — most of it was our own over-tight solver, not the digitized envelope. **A FALSE EQUILIBRIUM FOUND 2026-09-12, and the printed criterion is kept anyway.** Running the Figure 10 chop out past ~25 s — twenty times Ballin's 4.5 s of record — the real-time frame settles at **75.7 %NG, T41 1617 degR** while the differential model trims at **67.0 %NG, T41 1762**. Two roots, and the frame picks the wrong one. It is the **P45 tolerance**, not the pass cap: 1e-3 (printed) settles false at caps of both 8 and 20, while 1e-6/cap 40 and 1e-10/cap 200 both reach 66.895 %NG against the differential 67.04. The mechanism is this row's own subject — at 125 lbm/hr `f9` is asked for pressure ratios outside its table **136,704 times** in a 200 s run, and a loose tolerance on a clamped, therefore nearly flat, iteration function is exactly how a false fixed point appears. **Not changed**: 125 lbm/hr is below flight idle, and the report states that fuel control below flight-idle power was one of the features eliminated from the real-time model [pdf p.38], so this is outside the envelope the printed criterion was chosen for. Pinned by `test_the_printed_p45_tolerance_admits_a_false_equilibrium_below_flight_idle` so it stays known.|
| 46 | **Table B.1 and the Figures 6-7 sweep are not the same dataset.** They disagree systematically, and the disagreement grows as power falls: Table B.1 vs Fig. 6 is −1.50/−1.18/−0.83 %NG at Wf = 267.7/349.3/476.3 lbm/hr, and Table B.1 vs Fig. 7 is **+5.49/+1.32/−0.14 %** on shaft horsepower over the same three points. Both converge toward agreement at high power. | Interpreting every steady-state comparison, and the descent residual in particular | Ruled out, in order: **NP/load** — sweeping 16000-22000 rpm moves the NG-vs-Wf curve by <0.01 %NG, the gas generator being decoupled from NP at steady state; **the shared x axis** — Fig. 7's power agrees with our model to +1.66 %, and an x shift large enough to fix Fig. 6 makes it worse (−1.31 → −2.99 %NG); **a y-axis bow on p.40** — that page prints no detectable interior ticks (the ~3 px marks are inseparable from frame thickness), but both axes are anchored at printed values AT the frames (100/900 and 65.0/105.0), so a linear map is exact at both ends and any unmeasured bow is bounded near 0.04-0.08 %NG, an order of magnitude too small | **open.** Our model matches Table B.1 — printed numbers, zero digitization, the highest provenance in the report — to ±0.13 %NG and ±0.05 % shp at hover and level. **The consequence for the descent: at Wf = 267.7 lbm/hr Ballin's own two sources differ by 5.49 % on shaft power, while our model sits 0.73 % from Table B.1 and 5.0 % from Fig. 7 — i.e. comfortably inside the report's own internal spread at that condition.** Chasing the descent residual below that spread is chasing noise in the source. **Retracted:** an earlier entry claimed the linear map on p.40 misses its printed bottom frame by 2.34 %NG. That was computed from seven 'interior ticks' that were in fact data markers; p.40 has no usable interior ticks at all. **EXTENDED 2026-09-11 — it is a whole-curve systematic, not three points.** Figures 6-8 had been digitized since Phase 1 and **no test had ever used them**; every comparison in the repository sat at the same three operating points, which cannot distinguish a model that is right from one that is right *there*. Sweeping the full printed range: **Figure 8** (Ps3 vs %NG) agrees to **1.6 % worst over 24 points** — the strongest independent check we have, since no fuel flow enters it, so the compressor, station-3 volume and bleeds are validated without the combustor. **Figure 7** (shp vs Wf) is within 5 % at 12 of 14 solvable points, the exception being +11.3 % at 224 lb/hr decaying monotonically to -0.8 % by 700 — the same low-power conditioning as the descent trim, at the same end where this row already records Table B.1 and Fig. 7 differing by 5.49 %. **Figure 6** (%NG vs Wf) sits a systematic **-1.38 %NG** below across the entire 136-800 lb/hr range, narrowing monotonically from -2.0 %NG at 157 to -0.4 % at 709. At the three flows this row already measured, our sweep gives -1.55 / -1.07 / -0.83 against the -1.50 / -1.18 / -0.83 recorded for Table B.1 vs Fig. 6 — i.e. **we sit on Table B.1 and inherit its entire quarrel with Figure 6**, now shown to be the whole curve rather than three samples. Locked in as `validation/test_steady_sweeps.py`. Coverage note: we trim every printed Figure 6 point but the last, 100.31 %NG, which is past `f1`'s 100 % top speed line; refusing to extrapolate there is correct.  **EXTENDED TO FIGURES 9 AND 10, 2026-09-12 — and Figure 6 is again the outlier.** At 775 lbm/hr, the settled condition of Figure 9, four of the report's own datasets state the same operating point and they disagree: Figure 6 gives 99.69 %NG against Figure 9's settled 98.79 (**-0.90 %**), Figure 8 gives Ps3 244.17 against Figure 9's 234.85 (**-3.82 %**), and Figure 7 gives 1724.8 shp against Figure 9's 1641.0 (**-4.86 %**). **We sit inside that spread on every quantity**: -0.26 % from Figure 6 and +0.65 % from Figure 9 on speed; -1.92 % from Figure 8 and +1.97 % from Figure 9 on Ps3; -0.91 % from Figure 7 and +4.15 % from Figure 9 on power. So the steady-state part of the Figure 9 gap is substantially the report disagreeing with itself, and **no model can match both** — moving onto Figure 9 would break the Table B.1 and Figure 6-8 agreement that is currently good. A sharper internal test isolates which figure is the odd one: Figure 9 states BOTH %NG and Ps3, and Figure 8 is the relation between them, so evaluating Figure 8 at Figure 9's own settled speed needs no third source and no model. It agrees to **-0.59 %** (ours: -1.01 %). Figures 8 and 9 are mutually consistent; it is Figure 6, the %NG-versus-fuel-flow relation, that carries the offset — exactly what this row already recorded against Table B.1. **THE LOW-POWER 'ERRORS' ARE THE FIGURES', NOT OURS — settled 2026-09-12 against printed numbers.** Measured on a low-power sweep, we looked bad twice over: **-2.0 to -2.7 %NG against Figure 6** at 150-300 lbm/hr, and **+4.4 to +10.7 % shaft power against Figure 7**. Table B.1 settles both, because it is printed numbers with no digitization in the path and its descent trim sits at 267.7 lbm/hr, inside that region. At 267.7: Table B.1 says 302.6 shp, **Figure 7 says 286.0 — 5.49 % below it** — and we say 300.3, i.e. **-0.77 % from Table B.1 and +4.99 % from Figure 7**. Same for speed: our NG matches Table B.1 to +0.011 / +0.131 / -0.027 % at the three trims, while this row already records Figure 6 reading 0.83-1.50 %NG ABOVE Table B.1 and widening as power falls. So our low-power steady state is right wherever printed numbers exist, and the apparent -2 % and +10 % are those two figures' own offsets extrapolated below 267.7 lbm/hr. **Consequence for #47**: the Figure 10 decay problem is purely dynamic. There is no steady-state error underneath it to fix first.|
| 47 | **Our transient is too fast and spikes turbine temperature far too hard**, even though its endpoints match. On Figure 9's 400→775 lbm/hr step the 63 % rise times are: PCNG ours 0.203 s vs Ballin 0.290 s (**1.43×**), T41 0.007 vs 0.012 s (1.65×), **TORQ45 0.056 vs 0.194 s (3.47×)**. T41 overshoots its own settled value by **+400.8 °R** against Ballin's +120.6 °R — 3.3× too much. | Phase 4 fidelity; any closed-loop work in Phase 5 | Found by plotting the traces; the endpoint tests pass at 0.9–3.9 % and are blind to it | **partly closed** — 65 % of it accounted for. Original note: the leading suspect is the heat-sink model (Eqs. 48–53), which we do not implement.** We set `T41 = T41_ns`, which the report sanctions only when no heat-sink representation is used. Thermal inertia in the station-4.1 metal is exactly what would absorb a step in combustor temperature and damp the spike, and T45 feeding θ45 and hence ΔH_PT would then lag, which is where the torque discrepancy is largest. **PARTIALLY CLOSED 2026-09-11 — the heat sink was two thirds of it, and the comparison was also mis-configured.** Two findings. First, **Figures 9/10 were generated with the heat-sink model ON** [inferred from pdf pp.20, 38, 47 — see `docs/notes/heat-sink-configuration.md`], so running our 5-DOF `T41 = T41_ns` model against them was a category error, not merely an incomplete model. Ballin published both configurations and says which is which: Table 1 and Appendix B figs B1-B6 are heat-sink OFF [printed, pdf p.27 and p.67], B7-B12 are ON [printed, pdf p.67]. Our eigenvalue comparison against Table 1 was therefore correctly configured all along. Second, implementing Eqs. 48-53 as a switch closes **65 % of the Figure 9 T41 peak error** (+320.5 → +111.6 °R), 65 % of T45, and 90 % of TORQ45; the T41 overshoot goes 400.8 → 196.1 °R against Ballin's 117.4, i.e. 3.41× → **1.67×**. It also lifts the Figure 10 NGc minimum from 67.64 to 70.94 % against Ballin's 74.2 %, reducing `f1@65` clamping from 510 to 441. **The remaining 1.67× is unexplained** and this row stays open for it; candidates not yet eliminated are the digitized compressor envelope, the Eq. 74 reading (#22, closed but worth revisiting under the heat sink), and the possibility that Figs. 9/10 used the NASA-Lewis test-engine function set rather than the specification set — the report states that substitution for Tables 2/3 [pdf p.39] but is silent on these figures. Confirmed inert at steady state by measurement, not algebra: the two configurations agree to ~1e-15 relative (`tests/test_heat_sink.py`).  **TWO CANDIDATES ELIMINATED 2026-09-12, by measurement.** (a) *The heat-sink constants cannot be the cause.* Eqs. 50-53 make T41 a lead-lag on T41ns with DC gain 1 and high-frequency gain `k = 1 - tau_b/tau_a`; a lead-lag of that form **cannot overshoot on its own** — on a step it jumps by `k*delta` and then approaches `delta` monotonically. So the overshoot must already exist in T41ns, and the constants can only scale it. Measured: the gas path alone overshoots +384.9 degR and the heat sink attenuates that to +201.5 (k_eff 0.524). Reaching Ballin's +117.4 would need k_eff = 0.305, a **42 % change — 5.7x the 7.3 % uncertainty #31 actually measures**, and in the opposite direction, since #31 finds our ratio 7.3 % LOW so correcting it makes the overshoot bigger. (b) *The volume dynamics are not a missing damper.* Integrating the full Eqs. 42-47 pressures with a stiff solver instead of solving them algebraically agrees with the quasi-steady scheme to 5-11 % of the step, all of it within ~40 ms of the edge, and to 0.11 % at settle — so our quasi-steady solve is a faithful limit of our own volume dynamics. And it moves the spike the **wrong way**: T41ns overshoot goes +384.9 -> +792.4 degR with the volume dynamics in. (c) *Not a frame artefact either.* Refining dt from 14 ms to 0.44 ms, the T41ns overshoot converges upward to +417 degR (385.0, 384.9, 397.5, 407.1, 412.6, 417.0) — at 7 ms we slightly UNDER-report it. (d) *Not the input.* Ballin's own Wf panel shows a true step: the 10-90 % edge measures 0.000 s on Figure 9 and one sample on Figure 10. (e) #22 re-confirmed under the heat sink, which this row asked for: the printed Eq. 74 reading gives T41ns +1111 degR against the prose reading's +385. **What remains**: the function tables in the excursion region (#45 — FAR reaches 0.0327 against `f6`'s tabulated 0.010-0.020) and the possibility that Figs. 9/10 used the NASA-Lewis test-engine set. Scale note, so the ratio is not read as worse than it is: in absolute terms our T41 peak is 2902 against Ballin's 2790 (+4.0 %) and our settled T41 2701 against 2673 (+1.0 %); the '1.67x' is a ratio of two small differences. **RE-MEASURED 2026-09-12 WITHOUT TIME ALIGNMENT, and the last named candidate eliminated.** The clamping hypothesis (#45) is dead: relaxing every clamp in turn moves the T41 peak by at most 7.1 degR of 112. The registration-independent comparison — pre-step plateau, each trace's own extremum, settled plateau — on Figure 9: PCNG +0.08 / +0.61 / +0.60 %, Ps3 -0.33 / +1.56 / +1.63 %, **T41 -0.42 / +4.36 / +1.19 %**, **T45 -0.61 / +4.43 / +1.29 %**, TORQ45 -0.42 / +3.40 / +3.58 %. Overshoot above settle: T41 ours +196.6 against Ballin's +107.3 (**1.83x**, superseding the 1.67x figure, which came from a mis-aligned and stray-contaminated extraction), T45 2.10x. **The decisive structural observation**: Ballin's PCNG, Ps3 and TORQ45 do not overshoot at all (+0.00, +0.30, +1.21 in their own units) and neither do ours (+0.02, +0.15, +0.52). There is no ringing anywhere in either model — our path is not oscillatory. The entire discrepancy is a **4.4 % error in peak combustor temperature**, at a fuel-air ratio 126 % beyond `f6`'s tabulated range, with the clamps proven inert. **Figure 10 is a different problem and should not be pooled with this one**: there our settled T41 is +11.5 % and T45 +17.4 % — a steady-state error at 125 lb/hr, far below Table B.1's lowest 267.7 lb/hr trim, which is #46's low-power conditioning rather than dynamics. Ballin's Figure 10 T41 falls to 1659 and settles at 1662 (no recovery); ours falls to 1609 and recovers 244 degR to 1853, which is a qualitatively different path and the sharpest single discrepancy in the project. **What is left, and it is ours not the report's**: no physical hypothesis survives, and the measurement precision needed to go further does not exist in our reference data until #51's two defects are fixed. **LARGELY CLOSED 2026-09-12 — the cause was our solver tolerance, not the physics.** After eliminating the heat-sink constants, the volume dynamics, the frame size, the input shape and the table clamps, the remaining lever turned out to be printed on pdf p.37 and ignored by us: the pressure iterations converge to **0.1 percent** in ten and eight passes, and ours ran to 1e-10 in up to forty. See #25. Adopting the report's own criterion: **Figure 10 settled T41 +11.5 -> +3.2 %, T45 +17.4 -> +3.4 %, NGc minimum 70.94 -> 74.64 % against Ballin's 74.2 %** — that figure was the worst disagreement in the project and is now within ~3 %. Figure 9 improves less: T41 peak +4.36 -> +3.98 %, overshoot ratio 1.83 -> 1.73x, T45 peak +4.43 -> +4.13 %. Two things are still unexplained and this row stays open for them, narrowed: (a) Figure 9's peak temperature remains ~4 % high with an overshoot 1.73x Ballin's, and (b) **the peak arrives at t+14 ms against his t+30 ms** — a timing difference, which #25's unprinted pass count would close (a single fixed pass gives t+21 ms and +145.9 degR) but which cannot be adopted without printed support. Eq. 50 was re-read off the raster and is exactly as implemented: `T41/T41ns = ((Mc_pm/hA_m - Mc_pm/W_g c_pg)s + 1)/((Mc_pm/hA_m)s + 1)`. **RE-MEASURED HONESTLY 2026-09-12, and the earlier numbers here were flattering.** The three-point comparison (pre-step plateau, own extremum, settled level) was chosen because those quantities need no time alignment, but it cannot see a wrong settling time or a wrong path, and it reported 1-4 % where the whole curve is 6-11 %. Integrating the error over the record, normalised by each panel's own excursion: **Figure 9** PCNG 9.7 %, Ps3 6.4 %, T41 10.5 %, T45 11.4 %, TORQ45 8.9 %; **Figure 10** PCNG 5.6 %, Ps3 5.9 %, T41 13.9 %, T45 15.8 %. Mean 9.8 %, worst single-point excursions to 64 % of range. That is the honest headline and it is now tracked by `validation/test_whole_curve.py` with a ratchet, not by three points. **Two further corrections.** (a) *Nothing on Figure 10 is settled at 4.5 s.* Every 'settled' Figure 10 number reported here, before and after the convergence fix, was measured over 4.0-4.6 s while Ballin's PCNG is still falling at -2.45 %NG/s and ours at -1.46. Run out to 60 s ours reaches 66.89 %NG, matching our own differential trim's 67.04 % — so the formulations agree and the apparent 12 % gap between them was this mistake. (b) *Host-frame sampling does not explain the peak.* Sampling our 7 ms run at the 14 ms host frame over every phase offset lowers the drawn peak by at most 9.5 degR of the 101 degR gap; our peak is too broad for a sampler to miss. **The sharpest remaining target is not the peak but the late decay on Figure 10**, and it correlates cleanly with `f1`'s speed-line spacing: while NGc is above 80 %, where Figure A1 prints lines every 2-3 points, our decay rate matches Ballin's to 7 % (ratio 0.93); the moment NGc falls below 80 % and the only bracket is the **15-point [65, 80] hole**, the ratio jumps to 1.44 and grows monotonically to 1.68 at 75.6 %NG. The obvious candidate — that linear interpolation across speed lines is wrong — was tested and **eliminated**: a shape-preserving cubic in the parameter direction makes the slope ratio worse (1.52 -> 1.86) while moving the trims and the Figure 6/7/8 sweeps by less than 0.05 %. So the correlation is established and the mechanism is not. **MECHANISM LOCALISED 2026-09-12: it is T41, and both figures agree on the sign.** Two methodological fixes got there. First, comparing decay rates at matched TIME was wrong — at a given instant our NGc and his differ by up to 0.9 points. At **matched NGc** the picture inverts into something far more specific: our deceleration is **too front-loaded**, 15-32 % too strong above 81 %NGc (ratio 0.85 / 0.80 / 0.68 / 0.75 at 88 / 86 / 84 / 82 %) and 20-100 % too weak below (1.20 / 1.40 / 1.64 / 2.02 at 80 / 78 / 76 / 75 %), with our peak deceleration -9.6 %NG/s at 86 % against his -7.7 %. Second, plotting T41 against NGc removes time entirely and shows why: **our T41 collapses 160 degR too far and too early** (-160.2 / -158.3 / -161.4 degR at 90 / 88 / 86 %NGc), reaching its minimum at t+0.44 s, then over-recovers to +55 degR by 75 %NGc. His falls monotonically with speed to a minimum at t+3.7 s and stays there. Too little turbine work early, too much late — exactly the front-loading. **Both figures now constrain the same constant in the same direction.** The station 4.1 lead-lag scales the fast part of every T41 excursion by `k = 1 - tau_b/tau_a`; Figure 9 steps up and we overshoot, Figure 10 steps down and we undershoot. Scanning a multiplier on `f_hs` and scoring T41 at matched NGc: Figure 9's RMS falls 123.6 -> 73.9 degR and Figure 10's 119.7 -> 54.5, and **both biases cross zero at f_hs x ~1.65** (k ~0.33-0.41 against our 0.628). Two figures stepping in opposite directions, with opposite-signed biases, agreeing on one multiplier — the same kind of consistency that made #6's `j_load` recovery credible. **Nothing was changed, and the reason matters.** Every input to that ratio has now been verified against the raster: Eq. 50 is `T41/T41ns = ((Mc_pm/hA_m - Mc_pm/W_g c_pg)s+1)/((Mc_pm/hA_m)s+1)`, Eq. 51 is `Mc_pm/hA_m = TC_T41 sqrt(T41)/W41^(4/5)`, Eq. 52 is `T41_sgn = f_hs(NGc)`, Eq. 53 is `Mc_pm/(W_g c_pg) = T41_sgn/W41`, `TC_T41 = 0.29` is printed in Table A.1, and **Figure A11 is digitized exactly**: markers at (65, 4.00), (70, 4.00), (75, 5.75), (80, 6.20), (85, 7.48), (100, 7.48) on a 3.50-8.00 axis, which our file reproduces to 0.003. There is no unit error, no inverted axis and no missing factor to find. **So this is now a conflict between two of Ballin's own heat-sink results**: Appendix B's figs B7-B12 imply `tau1/tau2` of 0.6845 / 0.6571 / 0.6270 at the three trims (#31), i.e. k LARGER than ours, while Figures 9 and 10 imply k about 1.7x SMALLER. Either the report's two heat-sink-ON results disagree, or our Appendix B recovery — itself an inference, see #31 — is wrong. That is the next thing to settle, and it is settleable: both are printed. **CAUSAL CHAIN CLOSED 2026-09-12, and two of my own numbers corrected.** *Correction one*: the T41 excursion is **not** 1.7x too large. Measured from trim rather than from settle: Figure 9 ours +712.0 degR against Ballin's +601.2 (**1.18x**), Figure 10 ours -570.1 against -534.8 (**1.07x**). The 1.73x figure was overshoot *above settle* — a small difference of large numbers, which amplifies a modest error. *Correction two*: the `f_hs x1.65` preference is not a statement about `f_hs`, and #31 explains why. So the size of the excursion is nearly right and the **shape** is wrong: 160 degR too cold at 86-90 % NGc, 55 degR too hot at 75 %, minimum at t+0.44 s against his t+3.7 s. **The link to the turbine is confirmed and quantified.** `theta41 = K_TH41_1*T41 + K_TH41_2` (Eq. 25), `dH_GT = theta41*f7` (Eq. 26), `W41 = K_WGT*P41/sqrt(theta41)` (Eq. 28), so turbine power `= dH_GT*W41` goes as `f7*P41*sqrt(theta41)` — **the square root**, so a T41 error is worth half what it first looks. But the deceleration is set by the NET of turbine minus compressor power, which is a small difference of large numbers: at 86 % NGc turbine 627.9 Btu/s against compressor 760.6, net **-132.7**. A 161.4 degR T41 deficit costs 30.5 Btu/s of turbine power — **23 % of that net**. Predicted against measured deceleration ratio (his/ours), from the T41 error alone: 88 % 0.82 vs 0.85, **86 % 0.81 vs 0.80**, 84 % 0.86 vs 0.68, 82 % 0.94 vs 0.75, 80 % 1.04 vs 1.20, 76 % 1.11 vs 1.64, 75 % 1.15 vs 2.02. **At 86-88 % NGc the T41 error accounts for the front-loading essentially completely**; below 84 % it accounts for about a third, so a second contributor lives at low speed — which is the region where `f1`'s [65, 80] hole and `f_hs`'s steep drop both sit. The remaining question is therefore narrow and well posed: **why is our T41ns 160 degR too cold in the first second of a fuel chop, when its value at trim is right to 0.4 % and its total excursion to 7 %?** The iteration policy of #25 was tested against it and is not the answer (matched-NGc RMS 123.6 -> 114.0 on Figure 9, 119.7 -> 116.0 on Figure 10 for a fixed single pass). **SOLVED 2026-09-12. Eq. 50 is a collapse that is only valid for constant coefficients, and we were integrating the collapsed form.** The two printed heat-transfer equations [pdf p.25] are `c_pm M dT_m/dt = h A_m (T_gi - T_m)` (48) and `c_pg W_g (T_gi - T_go) = c_pm M dT_m/dt` (49). With `T_gi = T41ns` and `T_go = T41` those are exactly `dT_m/dt = (T41ns - T_m)/tau_a` and `T41 = T41ns - (tau_b/tau_a)(T41ns - T_m)`. Eq. 50 collapses them into a lead-lag, **which requires tau_a and tau_b to be constant** — and Eqs. 51 and 53 make them functions of T41 and W41. We carried Eq. 50's lead-lag memory `x`, with `T41 = k*T41ns + x`. Substituting shows **`x = (tau_b/tau_a)*T_m`**, so `k` is baked into the stored state: every time the coefficients move, `x` refers to the old `k` and is stale. On a fuel step `k` swings 0.62 -> 0.67 within a few frames, and the stale memory amplified the T41 excursion beyond the `k` Eq. 50 prescribes. **Integrating T_m instead has no such artifact** — the state is a physical temperature and means the same thing whatever the coefficients do — and it is identical to the lead-lag whenever they are constant, so Appendix B, Table 1 and every trim are untouched (the three Table B.1 trims are bit-identical: +0.011 / +0.131 / -0.027 %NG). Measured, whole-curve RMS as a percent of each panel's own excursion: **Figure 9** pcng 9.74 -> **5.55**, ps3 6.38 -> **2.77**, t41 10.52 -> **2.96**, t45 11.40 -> **2.20**, torq45 8.91 -> **2.22**; **Figure 10** pcng 5.55 -> **2.33**, ps3 5.89 -> **2.20**, t41 13.88 -> **5.38**, t45 15.79 -> **7.01**. **Mean 9.78 -> 3.63 %.** The T41 overshoot against Figure 9 went 3.41x Ballin's with no heat sink, 1.67x with the collapsed lead-lag, and is now **0.87x**. The `f_hs x1.65` that both figures appeared to want was compensating for this artifact, which is why it conflicted with Appendix B and why #31's k stands. `validation/test_whole_curve.py`'s ratchet drops 17.0 -> 8.0 %. Row stays open only for the residual: Figure 10's T41 and T45 are still the worst panels at 5.4 and 7.0 %. **RESIDUAL LOCALISED 2026-09-12, and T45 is not a separate problem.** Figure 10's T45 looked like the worst panel and like a second defect. It is neither. T45 is purely algebraic from T41 and the turbine work — `h41 = K_H41_1 T41 + K_H41_2` (24), `dH_GT = theta41 f7` (26), `h44 = h41 - dH_GT` (27), `h45 = K_H45 h44` (29), `T45 = K_T45_1 h45 + K_T45_2` (30) — and every constant is printed, so the chain **inverts**: from a plotted (T41, T45) pair, `dH_GT = h41 - (T45 - K_T45_2)/(K_T45_1 K_H45)`. Both panels are plotted, so Ballin's turbine work is measurable. **It agrees with ours to between +0.0 and +2.1 % across Figure 10's entire decay**, and T45's error tracks T41's almost exactly (-34.8 against -34.2 degR at 90 %NGc, +13.6 against +15.7 at 77 %). T45 reads worse only because its excursion is 373 degR against T41's 538, so the same absolute error is a larger fraction. **And the gas path is exactly right.** At matched NGc, Ps3 agrees to +/-0.8 % (worst 1.7 %) and the corrected flow `f1` returns at each side's own pressure ratio agrees to **+/-0.3 %, often to 0.00 %** — so FAR and T41ns are right and nothing upstream of the heat sink is at fault. **The whole residual is the metal lag**, a clean crossover: -34 degR at 90 %NGc to +16 at 77 %, zero at ~81 %. Separating the two ways that can happen: a history error would need his metal at **2264.5 degR at 90 %NGc, hotter than his own pre-step trim of 2193.9** — and metal cannot warm while the gas is colder than it, so it is not the history. It is the **gain**: his `tau_b/tau_a` is ~10 % larger than ours at 90 %NGc and ~18 % smaller at 77 %, so it falls faster than ours as the engine decays — faster, in fact, than `f_hs` alone falls. **RE-MEASURED ON CLEAN TRACES 2026-09-12, and two earlier diagnoses were contamination artifacts.** With the digitizer fixed (#51), Figure 9 is essentially solved — T41 within +/-33 degR at matched NGc with no systematic trend, dH_GT within 4 %, whole-curve RMS pcng 2.57 / ps3 2.35 / t41 4.42 / t45 5.26 / torq45 2.38 %. Figure 10 leaves one clean signature. **Retracted: the 'front-loaded deceleration'.** On clean traces the deceleration at matched NGc matches to 1-4 % ABOVE 80 %NGc — ratios 1.04 / 1.01 / 1.02 / 1.01 at 88 / 86 / 84 / 82 %  — and then degrades monotonically BELOW 80 %: 1.12 / 1.18 / 1.29 / **1.40** at 80 / 78 / 76 / 75 %. So there is no crossover in the rate; there is a single-sided failure below 80 %NGc. **Also retracted: the claim that the compressor flow was verified to 0.3 %.** That comparison was circular — `f1`'s speed lines are flat over most of the pressure-ratio range, so at matched NGc the flow agrees by construction when both sides are evaluated with our own `f1`. What IS verified is Ps3: +/-1.7 % dynamically, and +/-1.0 % against Figure 8 in steady state down to 70.35 %NG. **What the T41 error can and cannot explain.** T41 at matched NGc runs -34 degR at 90 % to +16 at 77 %, crossing at 80.5 %. Through `theta41` that is worth 0.45 % of turbine power at 77 %, i.e. 1.6 Btu/s on a net of -43.2 — about **a tenth** of the 40 % deceleration error. So T41 is not the cause below 80 %; something worth ~17 Btu/s is, which is 4.9 % of turbine work or 4.4 % of compressor work. **Candidate localised, not fixed**: the gas-generator/power-turbine work split. Scaling `f9`, which sets P45 through Eq. 80 and hence P45/P41 and `f7`, moves the speed strongly — x1.06 takes the Wf = 225 lbm/hr trim from -1.98 % to -0.07 % against Figure 6 — while leaving shaft power almost unmoved (+10.69 to +10.42 %). That rules out a single cause for the two low-power symptoms, and #46 then showed the shaft-power symptom was Figure 7's own offset. Not a fix: `f9` is digitized and scaling it to fit is tuning. **THE FIGURE 10 DEFICIT IS BELOW THE REPORT'S OWN RESOLUTION — settled 2026-09-12 by an error budget, after eliminating every candidate.** *The mass flow is right.* Ballin's W41 is recoverable from his own plotted panels with **no function map in the loop**: `theta41` from his T41 (25), then Eqs. 28 and 78 closed on each other given his Ps3 and Wf, all constants printed in Table A.1. Our WA31 matches his to **+/-0.8 % on Figure 10** (+/-1.8 % on Figure 9) all the way down to 77 %NG. *`f1`'s low-speed level is exonerated jointly*: WA31 is verified independently of `f1`, and `WA31 = WA2(1 - B1 - B2 - B3 - K_B3)`, so `f1` and the bleeds can only be wrong as a compensating pair and the pair is pinned to 0.8 %. *The bleeds are exonerated one at a time.* The budget says the compressor torque needs **+1.4 %** at 76 %NGc. `f3` (B1, the large one at 0.1090) is confirmed against Figure A3 read at 300 dpi — thirteen glyphs, plateau just under the 0.11 gridline, descending to zero at 89 % — and the 1.35x that would fix Figure 10 is simply not available from the page; it would also leave the printed trims untouched, since all three sit above NGc 89 where B1 is zero. `f4` (B2) has no leverage at all: **x4 moves the deceleration ratio from 1.29 to 1.31**. `f5` (B3) x1.20 reaches ratio 1.10 but drives the Table B.1 trims from +/-0.13 % to -0.75 / -0.87 / -1.17 %NG. `f2`, through h3, x1.01 reaches ratio 1.04 — nearly exact — and drives the trims to -0.62 / -0.63 / -0.72 %. So **every route to the missing torque either is not available from its own figure or breaks a printed trim.** *The unprinted ambient of #36 does not do it either*: T2 = 538.67 degR reaches ratio 0.99 but degrades the pre-step state (Ps3 -3.25 %, T41 +2.53 %, PCNG +0.97 %) where standard day fits all three to better than 0.71 %, and the jump from 1.27 at 528.67 degR is abrupt enough to look like a clamp threshold rather than a trade. **Why none of it localises, quantitatively.** `dNG/dt` is the difference of two nearly equal torques, and every term in both is now verified to a few percent against Ballin's own data. Propagating W41 +/-0.8 %, dH_GT +/-2 %, WA2 +/-0.8 % and h3 +/-1.7 %: net **-14.35 +/- 28 %** at 86 %NGc, **-5.43 +/- 49 %** at 80 %, **-4.08 +/- 57 %** at 76 %. The deficit being chased is **+1 %, +12 %, +29 %** at those same speeds — below the propagated uncertainty everywhere, and growing in step with it, because both are driven by the net becoming a small difference of large numbers as power falls. **This is not resolvable with the data the report contains.** What would resolve it is one printed steady-state point below 85 %NGc: Table B.1's trims stop at 85.2 % and Figure 6 stops at 136 lbm/hr, so the whole region where the deficit lives has no printed anchor. **MASS CONSERVATION AUDITED 2026-09-12, and it exposed the real gap in the evidence.** Nothing had ever asserted continuity. The audit closes it and adds `tests/test_mass_conservation.py`. Closure is a genuine check rather than an identity, because the four streams are each set INDEPENDENTLY — WA2 by `f1` (Eqs. 7, 9), WA31 by the combustor orifice (18), W41 by the choked nozzle (28), W45 by `f9` (33, 34) — against three balances. Measured at five trims from 476.3 down to 175 lbm/hr, in both formulations: Eqs. 42, 43 and 44 each close to **1e-13 of WA2 or better**, and so does the global balance `(WA2 + Wf) - W45 - overboard`, where overboard = `WA2(B1 + B2) + WA2 K_B3 + WA2 B3 (1 - K_bl)`. Eq. 39 was re-read at 600 dpi and is exactly as implemented, including the `WA2(K_QC1 H3 - H2) + WA3 K_QC2 H3` grouping; the prose above it confirms the 0.71/0.29 split is the report's own empirical bleed accounting. **But the audit surfaced this**: the overboard loss runs 2.99 % of WA2 at hover, 2.99 % at level, 7.73 % at descent and **13.91 % at 175 lbm/hr**, driven entirely by `B1`, which Figure A3 makes zero above NGc 89 and 0.1089 below 78. Of the three Table B.1 trims, **two sit where B1 is exactly zero and the third sees only 0.047 — less than half the plateau.** So the quantity with the most leverage on the one remaining transient disagreement is also the one the printed data constrains least, and everything below NGc 78 where the plateau is fully in effect has no printed anchor at all. That is not a claim that B1 is wrong — Figure A3 is unambiguous and its thirteen glyphs reproduce to 1e-4 — but it is why the error budget above cannot be tightened from the report, and it is now pinned by a test so nobody rediscovers it. **Eq. 39 IS an exact two-stream work balance, not an empirical fudge — 2026-09-12.** The report calls the 0.71/0.29 split "an empirically determined function" for the interstage bleed [pdf p.25], and `K_QC1 + K_QC2 = 1.000` exactly. Writing W24 for the station 2.4 bleed, the physical two-stream work `WA3(h3-h2) + W24(h24-h2)` equals Eq. 39 **if and only if h24 = K_QC1 h3 = 0.71 h3** — verified numerically to 5e-13 at five trims. So the constant places the bleed port at the station whose enthalpy is 71 % of compressor discharge, which works out at 36-49 % of the enthalpy RISE above inlet, increasing with power. The compressor accounting therefore conserves energy exactly and is not a candidate for the missing torque. **THE RESIDUAL IS Ps3, AND THAT IS AS FAR AS THE REPORT GOES — 2026-09-12, proved constructively.** The compressor exit was the right place to look: `Ps3` has a measured gain of **~13x** on `dNG/dt`. It enters twice and both routes pull the same way — `T3 = T2 f2(Ps3/P2)` (10) into the compressor torque (39), and `WA31 = sqrt(P3(P3-P41)/(K_dpb T3))` (18) into the whole core flow, the strong route, because P41 tracks P3 so a small move in P3 moves the pressure DROP by much more. Measured at 76 %NG: substituting Ballin's plotted Ps3, 2.2 % from ours, changes `dNG/dt` by 29 %. **Constructive proof of attribution**: pinning P3 to his own trace and integrating NG, P41 and P45 removes the low-speed deficit *entirely* — the rate ratio at 76 %NG goes **1.29 -> 1.00** and at 75 % **1.38 -> 1.08**, and the NG error stops growing, holding a flat -1.2 to -1.6 % instead of running to -5 %. Forcing Wf from his own fuel panel as well adds nothing, so the caption's 125 lbm/hr is right. It also *breaks* the high-speed end, 1.01 -> 0.87 at 86 %NG, because there our computed Ps3 is the more accurate of the two readings. So **Ps3 accounts for the entire residual in both directions.** **And it cannot be improved.** Every input to it is verified against Ballin's own printed data: WA31 to 0.8 % with no map in the loop, Ps3 itself to 1.7 % dynamically and 1.0 % against Figure 8 down to 70.35 %NG, `f3` confirmed against Figure A3, Eq. 39 shown to be an exact two-stream balance, and **`f2` digitized to 0.025 % per point** (1 sigma 0.00045 on 1.78 at Ps3/P2 = 5) with knots every 1.0 in pressure ratio. The scale that would close Figure 10 is 1.0 % on `f2` — **40 sigma** — and a uniform version of it also drives the descent trim from -0.03 to -0.72 %NG, since that trim sits at Ps3/P2 = 7.75 while the decay runs down to 3.71. So 1-2 % on Ps3 is as well as a digitized figure can be read, and 1-2 % on Ps3 **is** 15-30 % on `dNG/dt`. The leverage is pinned by `test_ps3_has_about_thirteenfold_leverage_on_the_speed_derivative` so the argument stays measured rather than remembered.|
| 48 | **Two Figure 9 reference traces carry a spurious final sample.** `fig09_t41_model.csv` ends `4.47935,2675.22659` then `4.93716,2188.82175`; `fig09_torq45_model.csv` ends `4.42849,414.19940` then `4.91323,194.25982`. Both trailing points sit ~0.47 s after the previous sample in traces otherwise sampled every 0.003 s. | Any consumer of the Figure 9 reference data | The T41 one lands on **exactly** its own t=0 value (2188.82175) to five decimals — the digitizer snapped to the same pixel row. T41 cannot fall 487 °R in 0.46 s with fuel still at 775 lbm/hr; TORQ45 cannot halve there either. `fig10_t41_model.csv` has the same 0.47 s gap but its value (1671.2 after 1660.6) is consistent with the settling trend and is **not** flagged. | **closed 2026-09-12 — detected by the tool rather than annotated by hand, and there are five, not two.** The original note was hand-edited into two generated files and the next rerun wiped it, which is the real lesson: an annotation on a generated file is not durable. `tools/digitize_fig910.py` now **detects** the condition — a final sample separated from its predecessor by more than ten times the median sample interval — and emits the `# defect:` block itself. Run objectively it flags **five** traces at 153x, 162x, 156x, 974x and 33x the median, three more than a careful reading found; one of the extras is `fig10_t41_model.csv`, which had been examined and cleared because its trailing value looked consistent with the settling trend. It is not consistent with the **sampling**, which is what identifies the artefact. All five lie outside `validation/`'s `t < 4.6` window, so no test was ever wrong; the bound is now commented at `late_them` and the rule is in CLAUDE.md's enforcement table. |
| 49 | **Our 2-DOF and reduced-order-5-DOF linear models are identical; Ballin's differ.** Table 1 prints -2.69 vs -2.81 at hover, -2.23 vs -2.16 at level, -1.82 vs -1.83 at descent. Ours agree to ~1e-12 at all three. | Reproducing Table 1 columns 4 and 5 as distinct checks | Implicit differentiation: if `g(s) = f1(s, p(s))` with `f2(s, p) = 0`, then `dg/ds = A11 - A12 A22^-1 A21`, exactly the Schur complement that the order reduction forms. Linearizing an **exactly solved** quasi-steady system and residualizing the full Jacobian are the same operation, so the identity is a theorem, not a coincidence. | **open — a real difference, correctly diagnosed, deliberately not chased yet.** Ballin's 2-DOF comes from a *separately coded* nonlinear program [pdf p.27], not the exact algebraic limit: it carries Eq. 74's opened compressor iteration, a finite inner-loop count, and the independent P45 solve. So his 0.12 /sec hover spread measures **his quasi-steady numerics**, and reproducing it means linearizing our `realtime.step` *discrete map* rather than a Newton solve — which needs a discrete-to-continuous conversion (matrix log, or `(A_d - I)/dt` to first order). Worth doing: it would put a number on what the real-time approximation actually costs, which is the report's central engineering claim. Until then **Table 1 column 4 is not an independent check for us** and column 5 is the proper target for both. |
| 50 | **Fig. C30 (`F_HM7`, HMU acceleration fuel limit) is the one scheduling function still undigitized.** Seven curves, marked by printed digits 1–7, which **cross** around PCNGHL 88–92 — curve 4 runs above 5, 6 and 7 there. | Phase 5: `WFPAC = F_HM7(T2, PCNGHL)` is the acceleration limit, the schedule that stops an open-loop fuel step cooking the engine (see #45) | Measured, not assumed: ranking ink runs by y resolves seven separate curves in only **70 of 410** sampled columns, and those lie outside the crossing bundle. Each curve also drops to WFPAC = 1 at its own speed, so the knot abscissae differ per curve and no shared list describes them | open — needs per-curve **tracking**, following each line through a crossing by direction rather than by order, with the printed digits used to seed and to check the identities. `tools/digitize_appc_multi.py` refuses the figure by name rather than emitting something plausible. Everything upstream — frame, tick lattice, axis calibration — already works on this page |
| 51 | **The Figure 9/10 model traces carry off-curve samples, and the path comparison cannot be trusted until they are separated.** Across the ten dense `fig??_*_model.csv` traces (6,632 samples) a local-median test flags **61 samples that sit off their own curve** in a locally flat neighbourhood — stray ink, tick marks and, in six cases, the GE reference markers themselves. `fig09_pcng_model.csv` is the clearest: after t = 3.5 s its 130 samples fall into three populations — 122 at 98.73–98.79 (the curve), 4 at 96.92–96.98 (the GE marker level), and 4 at 90.33–90.36 (off-panel ink). | Any comparison of transient *shape* rather than endpoints — which is what open question #47 is really about | Detected 2026-09-12 by `tools/` analysis after three successive shape-metric attempts returned nonsense: Ballin's PCNG appeared to overshoot by 30–100 %, which Figure 9 plainly does not show | **closed 2026-09-12.** Original note: measured, not yet fixed.** The existing endpoint tests are unaffected in outcome (they pass, and they average over enough samples that 0.9 % contamination does not move them), but **shape metrics are not robust to it**: a single stray at the peak or inside the settling window moves overshoot and rise time by tens of percent. Three separate attempts at a rise-time/overshoot table were corrupted this way, and one also carried a sign error on the down-step peak — the metrics are only meaningful after the traces are cleaned in `tools/digitize_fig910.py`, not after the numbers are cleaned. Do not quote a path-shape comparison against Ballin until this closes. The separation looks tractable: the GE markers are `+` glyphs and structurally distinct from the solid model line, which is how `digitize_fig910.py` already tells the two series apart everywhere else — these are the leakage it misses.  **SECOND DEFECT IN THE SAME DATA, 2026-09-12: the panels disagree about when the step happened.** All six panels of a figure are one run and must share one step time. Detecting each panel's own response onset from its own samples: Figure 9 gives Wf 0.566, PCNG 0.554, Ps3 0.557, T41 0.536, T45 0.539, TORQ45 0.518 — a **48 ms spread**; Figure 10 spreads 36 ms. Each panel was digitized from its own frame with its own axis fit, so each carries its own registration; 48 ms is ~1 % of the 5 s axis, which is the order of a frame-fit error. **Why this is disqualifying rather than untidy:** the T41 peak occurs 9 ms after the step, so the registration error is 5x the interval being measured. Any quantity read at a fixed offset from a step time taken from another panel is meaningless — which invalidated our own first attribution of the #47 peak error to the compressor operating point. Registration-independent quantities (a pre-step plateau, a trace's own extremum, a settled plateau, and the overshoot built from the last two) are unaffected and are what #47 now quotes. Fixing this means calibrating all panels of a figure on one shared time axis rather than per panel. **CLOSED 2026-09-12 — the spikes were ours, in one line of the tracer.** This row reported the off-curve samples as a defect of the 1988 scan. They were a defect of `trace_line`, which filtered the ink runs in a column **before** counting them: `runs = [r for r in runs if r.size <= 6]` and then `if len(runs) != 1`. That discards a long run instead of rejecting the column, so a column carrying both a long run and a short one passed and the short one won — and that is exactly the geometry of these panels. The GE `+` markers are drawn **on** the model line, and each vertical bar has a slightly detached top, so a column through one holds a long run (bar merged with line) plus a short one at the bar's top. The long run was thrown away and the sample taken 40 px above the trace: 61 degR on the T45 panel. Counting the runs before filtering fixes it. **Two more causes found in the same pass.** (a) The trailing samples of #48 are re-acquired ink past the end of the plotted curve, and the separation is unambiguous — every artifact sits behind an x-gap of **more than 130x** the median sample interval while every legitimate continuation sits behind 20-25x — so the digitizer now **drops** them at a 50x threshold and records in the header exactly what it removed and why. The old note said 'consumers must window it out', and three of them grew their own filter, one of which was wrong. (b) `margin`, which excludes the frame ticks, was a guessed 20 px. Measured over all twelve panels, ticks reach 13 px at the 99th percentile; and Figure 9's PCNG settles at 98.79 %, which lands on row 1177 of a panel whose top is 1157 — exactly the old window edge. The trace was clipped out of those columns, leaving only a GE marker at 96.95, which the tracer then read as the curve and drew as a notch in the last 0.4 s of every PCNG overlay. 16 px clears the ticks and keeps the trace. **Result: off-curve samples 38 -> 2 in ~6,100**, and the two that remain are isolated single pixels. The downstream filters in `plot_validation.py` and `test_whole_curve.py` are gone — removing them was not tidying, the trailing-block one had begun condemning the last sixteen genuine samples of Figure 10's T41 trace where the curve really does flatten at 1659 degR. The per-panel time-registration worry recorded here earlier was also wrong and is withdrawn: the six panels share one fitted `left`/`right`, so the time axis is common by construction.  **MUTUAL CONTAMINATION FOUND AND FIXED, 2026-09-12 (second pass).** The first pass fixed the run-counting bug but left spikes, and the user identified the pattern that explained them: **GE data was being read as Ballin's and Ballin's as GE**, both directions. Two more causes. (a) *Markers inheriting the model's value.* `marker_centres` took the centre of mass of `horiz & vert`, and where a `+` sits **on** the model line — most of them, since Ballin plotted the GE data against his own curve — the horizontal stroke in that intersection *is the line*, so the centre was pulled onto it. Twelve GE samples read the model curve to within a pixel. The centre now comes from the **vertical** stroke alone, which belongs only to the glyph, with the horizontal kept as a presence test; the stroke's height bounds are measured, 19-25 px against 85-212 px for the trace's own steep segments. (b) *The trace accepting glyph debris.* Thinness is not enough: a detached bar-top is itself a thin single run. The trace is a **connected** line, so candidates are now split wherever adjacency breaks and the blocks linked by slope from a backbone of the long ones. Two wrong rules were tried and are recorded in the code: a minimum block LENGTH threw away Figure 9's T41 peak — a legitimate 7-column block holding the 2790 degR maximum, the most-quoted number in the comparison — because blocks are only ~14 columns long, the markers sitting every 33 px with 18 columns masked around each; and slope alone let a **1-column** block at the GE level back in on Figure 9's TORQ45, linking at 0.83 px per column simply because the gap to the next real block was long. Both together separate the two cleanly, 7 columns against 1. (c) One isolated GE detection remained, five lattice pitches past the end of Figure 10's T45 series, so `snap_to_lattice` now trims ends separated by more than 2.5 pitches. **Result: zero off-curve samples, zero adjacent-column jumps above 6 quanta, and zero cross-contamination across all twelve traces.** The two remaining coincidences are at t ~ 0.5 where the GE markers and the model curve genuinely cross at the step. Every printed level and every peak is preserved: Figure 9's T41 max is 2790.03 at t=0.575 as before.|
| 52 | **Eq. 29 does not conserve energy, by construction, and the report says so.** *"At station 4.5, gases from the station 4.4 and cooling-bleed flow from the compressor are mixed before passing through the power turbine. The enthalpy of the mixed gases is proportional to enthalpy [at] station 4.4"* [pdf p.24]. So Eq. 29 is `H45 = K_H45 H44`, a single multiplicative fraction with `K_H45 = 0.9623`, where a flow-weighted mix of the two streams would give `h45 = (W41 h44 + B3 K_bl WA2 h3)/W45`. | Interpreting T45, shaft power, and the overall energy balance | Found 2026-09-12 by a full station-by-station mass and energy audit; Eq. 29 re-read on pdf p.24 | **closed 2026-09-12 — reproduced as printed, quantified, and asserted.** The coefficient that WOULD conserve energy is **0.9740 / 0.9747 / 0.9775 / 0.9788 / 0.9774** at Wf = 175 / 200 / 267.7 / 349.3 / 476.3 lbm/hr — strikingly constant — against the printed **0.9623**. So the printed value sits **1.1 to 1.7 % below** an energy-conserving mix, and that deficit is the entire reason the engine's overall energy balance does not close: in 3377.10 against out 3325.66 Btu/s at hover, -1.52 %. Everything else closes exactly — mass at every station to 1e-13 of WA2, the combustor balance `WA31 h3 + eta_b Wf HVF = W41 h41ns` to 1e-13, and Eq. 39 to 5e-13 (see below). It affects only the power turbine, since h45 feeds T45, theta45, dH_PT and W45, so it does not touch the gas generator dynamics of #47 — and our T45 tracks Ballin's, which says his model carries the same approximation. Asserted by `tests/test_mass_conservation.py::test_station_4_5_mixing_loses_energy_as_the_report_specifies` rather than corrected, so a re-reading of `K_H45` or an error of ours would fail rather than pass quietly.  **BETA INTERPOLATION MEASURED IN FULL AND NOT SHIPPED — 2026-09-12.** Figure A1 prints six dotted diagonals threading the markers of all eleven speed lines, and our seven points per line sit on them, so interpolating along them rather than at constant Ps3/P2 is the structurally correct construction — constant-PR provably blends non-corresponding points (see #43). Scored on everything rather than on the metric it was reached for: **better** — worst trim speed 0.131 -> 0.120 %, **Figure 8, the strongest independent steady check since no fuel flow enters it, 1.625 -> 1.184 %**, worst whole-curve transient panel 8.196 -> 8.030 %, Figure 10 deceleration ratio at 76 %NGc 1.29 -> 1.20; **worse** — worst trim shaft power 0.772 -> 0.912 %, Figures 6 and 7 by 0.07 and 0.21 points, **Ps3 parity at 76 %NGc +2.17 -> +3.17 %** (the thing it was tried for), and the Appendix B `d(.)/dNG` column **redistributed rather than reduced**: hover -0.62 -> -2.54 %, level +7.53 -> **+20.68 %**, descent -10.94 -> **+2.87 %**, with median element errors 6.83/4.49/5.66 -> 6.08/9.13/2.20. It also fails four tests in `test_appendix_b_elements.py` that encode #43's staircase diagnosis — and that diagnosis is itself evidence about where the error lives, so losing it costs something. **Held, not shipped**: the structural argument is sound and stands on its own, but the evidence is a wash that moves error around, and this project's rule is not to change a scheme on neutral evidence. Recorded in full so the decision is reversible with the numbers in hand; the implementation is a 12-line change to `SpeedMap.__call__`.|
